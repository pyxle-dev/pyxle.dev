{"title":"Changelog","description":"Release notes for Pyxle. While we're in beta (`0.x`), minor versions may include breaking changes — those are called out explicitly here. To upgrade, run `pip install --upgrade pyxle-framework`.","html":"<p>Release notes for Pyxle. While we&#39;re in beta (<code>0.x</code>), minor versions may include breaking changes — those are called out explicitly here. To upgrade, run <code>pip install --upgrade pyxle-framework</code>.</p>\n<h2 id=\"unreleased\">Unreleased</h2><ul>\n<li><p><strong>The <a href=\"/docs/guides/comparison\">comparison guide</a> now names what Pyxle doesn&#39;t have yet.</strong> A new &quot;What Pyxle doesn&#39;t have yet&quot; section honestly lists the current edges — authoring the client in TypeScript (end-to-end Python types work today; TSX authoring is the <a href=\"/docs/guides/fully-typed-pyxle\">next milestone</a>), a first-class test client, automatic image byte- and font-optimization, and i18n — with how to bridge each one now, so the gaps read as roadmap rather than surprises.</p>\n</li>\n<li><p><strong>A fresh project now ships a human <code>README.md</code>.</strong> <code>pyxle init</code> scaffolded an <code>AGENTS.md</code> for AI agents but no README for people; new projects get a README covering prerequisites, the run commands, and the project layout. The <code>--template</code> help text no longer says &quot;(placeholder)&quot; — it accurately notes only <code>default</code> is supported today.</p>\n</li>\n<li><p><strong>New <a href=\"/docs/guides/debugging\">Debugging guide</a>, and a more accurate Deployment guide.</strong> The Debugging guide covers both runtimes — reading the error overlay, <code>breakpoint()</code>/<code>pdb</code> in a loader, server logs in the browser console, the SSR (Node) side (<code>window is not defined</code>, <code>console.log</code> running twice, the Node inspector), and reading the compiled <code>.pyxle-build/</code> output. Deployment fixes: the sample Dockerfile installs Node.js <strong>22.x</strong> (the old <code>setup_20.x</code> can ship a below-floor 20.16–20.18 that Vite 7 rejects); the metrics endpoint is documented at its real path <code>/api/__pyxle/metrics</code> (opt-in via <code>metricsEndpoint: true</code>), not <code>/metrics</code>; a new <strong>Database migrations</strong> section covers <code>pyxle-db</code>&#39;s startup/<code>pyxle-db migrate</code> flow and backward-compatible migrations for zero-downtime deploys; and a <strong>blue-green gotcha</strong> callout explains that the two instances must share an explicit <code>csrf.cookieName</code> and the same <code>PYXLE_SECRET_KEY</code> or mid-cutover POSTs <code>403</code>.</p>\n</li>\n<li><p><strong><code>pyxle serve</code> refuses to start in production without <code>PYXLE_SECRET_KEY</code>.</strong> Serving without a signing secret meant CSRF tokens fell back to unsigned double-submit and every signed cookie (sessions, unsubscribe links) was forgeable — previously only a single easily-missed startup warning. <code>pyxle serve</code> now fails fast with an actionable message and a one-liner to generate a key. <code>pyxle dev</code> still allows a missing key (it uses a public dev fallback), and the check is skipped if you&#39;ve deliberately set <code>csrf.enabled=false</code>. See <a href=\"/docs/guides/security\">Security</a> and <a href=\"/docs/guides/deployment\">Deployment</a>.</p>\n</li>\n<li><p><strong>Security docs now reflect what Pyxle actually sends.</strong> The <a href=\"/docs/guides/security#security-headers\">Security guide</a> previously told you to add <code>X-Content-Type-Options</code>, <code>X-Frame-Options</code>, and <code>Referrer-Policy</code> at your proxy — but Pyxle already sets all three on every response (and uses <code>X-Frame-Options: SAMEORIGIN</code>, not the <code>DENY</code> the old docs showed). The guide now documents the auto-set baseline and covers only the two headers genuinely left to you: <code>Content-Security-Policy</code> (app-specific) and <code>Strict-Transport-Security</code> (belongs at your TLS layer). A <a href=\"https://github.com/pyxle-dev/pyxle/blob/main/SECURITY.md\" target=\"_blank\" rel=\"noreferrer\">vulnerability-disclosure policy</a> (<code>SECURITY.md</code>) is now published for every Pyxle repository.</p>\n</li>\n<li><p><strong>A real testing story: <code>pyxle.testing</code> helpers and a Testing guide.</strong> &quot;How do I test my app?&quot; now has a runnable answer. A <code>.pyxl</code> file isn&#39;t an importable Python module, so the old advice (<code>from pages.index import load_home</code>) raised <code>ModuleNotFoundError</code>. New <code>pyxle.testing.load_loader(&quot;pages/index.pyxl&quot;)</code> compiles the page and returns its <code>@server</code> loader as a plain <code>async</code> function you can call with a fake request; <code>load_page(...)</code> returns the whole module so you can reach any <code>@action</code> by name. A new <a href=\"/docs/guides/testing\">Testing guide</a> covers unit-testing loaders/actions, the keep-logic-in-plain-modules pattern, and end-to-end tests over HTTP (with the CSRF handshake), and the <a href=\"/docs/architecture/runtime\">runtime internals doc</a> no longer shows the broken import. A first-class in-process <code>PyxleTestClient</code> is called out as the next step on the roadmap.</p>\n</li>\n<li><p><strong><code>pyxle dev</code>/<code>build</code> now check your Node.js version up front instead of failing later as an opaque Vite crash.</strong> If Node.js is missing or older than the required <code>20.19</code> (the Vite 7 floor), the command stops immediately with a clear, actionable message — <code>Node.js 20.19+ is required, but 20.16.0 is installed.</code> — rather than printing &quot;ready&quot; beside a red Vite fatal and crash-looping. <code>pyxle init</code> warns about a below-floor toolchain after scaffolding (non-fatal, since scaffolding needs no Node), and <code>pyxle serve</code> warns when serving a prebuilt app on an old Node. The dev server also no longer prints its &quot;ready&quot; banner if Vite exits during startup. <a href=\"/docs/getting-started/installation#troubleshooting\">Installation</a> gains a Troubleshooting section mapping the two most common toolchain errors — pip&#39;s &quot;No matching distribution found&quot; (Python below 3.10) and the Node.js version error — to the fix, and <a href=\"/docs/getting-started/quick-start\">Quick Start</a> restates both version floors up front.</p>\n</li>\n</ul>\n<h2 id=\"070-2026-07-09\">0.7.0 — 2026-07-09</h2><ul>\n<li><p><strong>Fix: <code>error.pyxl</code> renders even when an ancestor layout has a <code>@server</code> loader.</strong> The error boundary is compiled wrapped in its layout chain like any page, but its render received no layout loader data — the layout component crashed on the missing props and the request silently fell back to the built-in error document (in dev <em>and</em> production, with no log). Boundary renders now execute ancestor layout loaders exactly like a normal page; a loader that fails during the boundary render no longer masks the boundary (it renders with error-only props and the failure is logged), and a boundary that itself fails now logs the fallback instead of swapping silently. Also: honest scope wording for <code>pyxle typecheck</code> (page JSX isn&#39;t checked without <code>checkJs</code> — planned, see <a href=\"/docs/guides/fully-typed-pyxle\">the typed-Pyxle roadmap</a>), the pyxle-auth doc header says 0.4.0, the <code>useAuth</code> seed claim matches real SSR behavior, the <code>username-available</code> <code>reason</code> field is documented, six missing guides join the docs index, and three dead anchors are fixed.</p>\n</li>\n<li><p><strong>Pre-release verification fixes.</strong> <code>PYXLE_SSR_WORKER_CONCURRENCY</code> now actually reaches the SSR worker (the sanitized spawn environment stripped it, making the documented knob a no-op). <code>SIGTERM</code> to <code>pyxle dev</code> — what process managers and IDE stop buttons send — tears down the whole child tree (Vite, esbuild, SSR workers) exactly like Ctrl-C instead of orphaning Vite on its port. A failed rebuild now broadcasts to the browser overlay (as the architecture docs promise), and reverting a file to its last-good content after a failed build correctly triggers the recovery rebuild instead of being hash-skipped. Assorted doc corrections: the overlay WebSocket path (<code>/__pyxle__/overlay</code>), the scaffold&#39;s generated <code>.env.local</code> in the project-structure tree, a verbatim <code>pyxle routes</code> sample, a legacy-Tailwind-v3 note in the styling guide, and MiB units in the security guide.</p>\n</li>\n<li><p><strong>Fix: React UI libraries that resolve to a CommonJS build no longer crash SSR.</strong> A dependency without an <code>exports</code> map (e.g. <code>lucide-react</code>, common across the shadcn/ui ecosystem) resolved to its CommonJS build during the server render, whose <code>require(&#39;react&#39;)</code> failed with <code>Dynamic require of &quot;react&quot; is not supported</code> — a 500 on any page importing it. The SSR bundler now resolves dependencies <strong>ESM-first</strong> (prefers the <code>module</code>/ESM entry over CommonJS <code>main</code>, as Vite&#39;s SSR does), so those packages link cleanly against the runtime&#39;s React. A genuinely CommonJS-only package that still <code>require</code>s a runtime module now fails with an actionable error naming the module and the page, pointing at an ES-module version or <code>&lt;ClientOnly&gt;</code>, instead of the bare esbuild message. See <a href=\"/docs/guides/third-party-packages#commonjs-packages-and-ssr\">Third-party packages → CommonJS packages and SSR</a>.</p>\n</li>\n<li><p><strong>Docs: the road to a fully-typed Pyxle.</strong> A new <a href=\"/docs/guides/fully-typed-pyxle\">roadmap page</a> lays out where Pyxle&#39;s type story is heading — authoring your client in TypeScript and forwarding Python types across the boundary so your component&#39;s <code>data</code> is typed from the Python that produced it — grounded in what already works today (<code>pyxle check</code>, <code>pyxle typecheck</code>, Pydantic <code>@action</code> validation). JavaScript remains fully first-class; nothing here changes today&#39;s behavior.</p>\n</li>\n<li><p><strong>Breaking prerequisite: Node.js 20.19+ is now required.</strong> Node 18 is end-of-life; Vite 7 (below) needs Node <code>&gt;=20.19</code>. Update your Node before upgrading. The scaffold now declares <code>&quot;engines&quot;: { &quot;node&quot;: &quot;&gt;=20.19&quot; }</code>, and <a href=\"/docs/getting-started/installation\">Installation</a> states the new floor.</p>\n</li>\n<li><p><strong>Modernized scaffold — React 19, Vite 7, and an interactive <code>pyxle init</code>.</strong> A fresh project now ships <strong>React 19</strong> and <strong>Vite 7</strong> (with the Vite-7-compatible <code>@vitejs/plugin-react</code> 5), and <code>pyxle init</code> is <strong>interactive</strong>: polished arrow-key prompts (highlighted defaults, Enter to accept) ask whether you want Tailwind CSS, whether you want shadcn/ui (only when Tailwind is on), and whether to customize the default <code>@/*</code> import alias — the value input appears only if you opt in — then scaffold only what you chose. Non-interactive callers (CI, scripts, non-TTY stdin) never block — pass <code>--yes</code> for defaults, or the explicit <code>--tailwind/--no-tailwind</code>, <code>--shadcn/--no-shadcn</code>, and <code>--import-alias</code> flags. <code>pyxle init .</code> (or <code>pyxle init</code> in an empty directory) scaffolds into the current directory and derives the project name from it. Fresh installs pass <code>npm audit</code> clean. See <a href=\"/docs/getting-started/quick-start\">Quick Start</a>.</p>\n</li>\n<li><p><strong>Tailwind CSS is now opt-in — and, when chosen, Tailwind v4 wired straight into Vite.</strong> Previously every project got Tailwind v3 through a side-channel (<code>tailwind.config.cjs</code>, <code>postcss.config.cjs</code>, a pre-compiled <code>public/styles/tailwind.css</code>, and a standalone <code>tailwindcss --watch</code> process). That whole mechanism is gone. Choose Tailwind at <code>pyxle init</code> and you get <strong>Tailwind v4</strong> via the <code>@tailwindcss/vite</code> plugin and a single <code>@import &quot;tailwindcss&quot;;</code> CSS entry — no config files, no separate watcher, no output-CSS-in-<code>public/</code>, and therefore no &quot;Tailwind writes into a watched dir&quot; rebuild loop. Pyxle adds the plugin to the Vite config it generates only when the project depends on <code>@tailwindcss/vite</code>. Decline Tailwind and you get a clean baseline where <strong>plain CSS and CSS Modules</strong> work out of the box (both verified in dev and build). CSS Module class names are generated deterministically, so the server-rendered markup and the client bundle match exactly — no React 19 hydration mismatch. See <a href=\"/docs/guides/styling\">Styling</a>.</p>\n</li>\n<li><p><strong>shadcn/ui support.</strong> Choose shadcn at <code>pyxle init</code> (it implies Tailwind) and the scaffold pre-configures <code>components.json</code>, the <code>@</code> import alias, <code>lib/utils.js</code>, and a Tailwind v4 stylesheet with the shadcn theme — so <code>npx shadcn@latest add button</code> works with no <code>shadcn init</code> step. Every project also gets a root <code>vite.config.js</code> (re-exporting Pyxle&#39;s generated config) and a <code>jsconfig.json</code>, so the Vite ecosystem and editors find a config and resolve the import alias. Pyxle resolves that alias in both the Vite client build <strong>and</strong> the SSR runtime, so aliased imports render on the server too. See <a href=\"/docs/guides/third-party-packages#shadcnui\">Third-party packages → shadcn/ui</a>.</p>\n</li>\n<li><p><strong>Fix: streaming SSR no longer serializes across requests.</strong> Concurrent requests to a page that streams (<code>&lt;Suspense&gt;</code> / <code>loading.pyxl</code>) used to render strictly one at a time: each connection&#39;s first byte arrived only after the <em>previous</em> stream had fully finished, so several visitors hitting the same slow page saw a time-to-first-byte staircase (roughly 0.5s, 1s, 3s, 6s…) even though each render was mostly idle waiting on its own boundaries. An SSR worker now handles many renders <strong>concurrently</strong> — overlapping requests all begin rendering immediately and interleave, so every visitor gets the shell in tens of milliseconds regardless of how many others are mid-render. Each render&#39;s per-request state (pathname, CSRF token, <code>&lt;Head&gt;</code> tags, inline styles) is isolated with <code>AsyncLocalStorage</code>, so interleaving can never render one visitor&#39;s token or head tags into another&#39;s page. As part of this, <code>pyxle serve</code> now <strong>auto-sizes</strong> its SSR worker pool by default (previously a single process), so production renders in parallel out of the box; <code>pyxle dev</code> still runs a single worker. In-worker render concurrency defaults to 16 and is tunable with <code>PYXLE_SSR_WORKER_CONCURRENCY</code>. See the <a href=\"/docs/guides/streaming#concurrent-streams-dont-queue\">Streaming guide</a>, <a href=\"/docs/guides/deployment#ssr-workers\">Deployment → SSR workers</a>, and <a href=\"/docs/guides/environment-variables#ssr-rendering\">Environment variables</a>.</p>\n</li>\n<li><p><strong><code>pyxle init</code> no longer scaffolds a stale framework pin.</strong> The generated <code>requirements.txt</code> used to hard-code <code>pyxle-framework&gt;=0.4.1,&lt;0.5</code>, so <code>pyxle install</code> in a fresh project silently <em>downgraded</em> the framework that scaffolded it. The requirement is now rendered from the running version — running <code>0.7.0</code> emits <code>pyxle-framework&gt;=0.7.0,&lt;0.8</code> (current release up to the next minor).</p>\n</li>\n<li><p><strong><code>pyxle typecheck</code> now fails fast with a clear error when TypeScript isn&#39;t installed.</strong> It previously fell back to <code>npx --yes tsc</code>, which resolves npm&#39;s placeholder <code>tsc</code> package — relaying its banner and ending with a self-contradictory &quot;Type check failed with 0 error(s)&quot;. The command now checks for a real <code>tsc</code> (local <code>node_modules/.bin</code>, then global) <em>before</em> building and exits with <code>TypeScript is required for &#39;pyxle typecheck&#39; — run: npm install --save-dev typescript</code> (or &quot;run: npm install&quot; when it&#39;s declared but not installed). A failed <code>tsc</code> run that produces no diagnostics now reports the exit code instead of &quot;0 error(s)&quot;. See <a href=\"/docs/guides/typescript\">TypeScript guide</a>.</p>\n</li>\n<li><p><strong>Scaffolded <code>AGENTS.md</code> corrected on auto-injected names.</strong> It claimed <code>LoaderError</code> had to be imported from <code>pyxle.runtime</code> — false since 0.6.1, and contradicted by the rest of the file. It now consistently documents that <code>server</code>, <code>action</code>, <code>LoaderError</code>, <code>ActionError</code>, <code>ValidationActionError</code>, and <code>invalidate_routes</code> are all auto-injected by the compiler.</p>\n</li>\n<li><p><strong><code>pyxle check</code> works out of the box — <code>pyxle-langkit</code> is now a default dependency.</strong> On a fresh install, <code>pyxle check</code> — quick-start step 6 — exited 1 on a brand-new scaffold with &quot;JSX checker unavailable&quot; because the JSX layer lived behind the undocumented <code>[langkit]</code> extra. The extra is no longer needed: <code>pip install pyxle-framework</code> now pulls in <code>pyxle-langkit</code>, so the full check (Python syntax, semantics, and JSX) passes on generated files with zero extra steps. The <code>[langkit]</code> extra remains for backward compatibility but is now redundant. See <a href=\"/docs/getting-started/installation\">Installation</a> and <a href=\"/docs/getting-started/quick-start\">Quick Start</a>.</p>\n</li>\n<li><p><strong>Fix: the CSRF cookie no longer collides between Pyxle apps on the same host.</strong> The cookie was literally <code>pyxle-csrf</code> with <code>Path=/</code>, and browsers scope cookies to a host — ignoring the port — so two Pyxle dev servers on <code>127.0.0.1</code> kept overwriting each other&#39;s token and every action in the other app failed with <code>403 CSRF token mismatch</code>. The default cookie name is now namespaced by the app&#39;s bind port (<code>pyxle-csrf-8000</code>), a stable per-app discriminator in dev <em>and</em> behind a production reverse proxy, and the document shell injects the effective name into every page (<code>window.__PYXLE_CSRF_COOKIE__</code>) so <code>&lt;Form&gt;</code>/<code>useAction</code> always read the right cookie. Pin an explicit name with the existing <code>csrf.cookieName</code> key. <strong>Upgrade note:</strong> a leftover <code>pyxle-csrf</code> cookie from an earlier version is simply ignored — the token re-issues under the namespaced name on the next page load; no action needed. See <a href=\"/docs/guides/security#csrf-protection\">Security → CSRF protection</a> and <a href=\"/docs/reference/configuration#csrf\">Configuration → CSRF</a>.</p>\n</li>\n<li><p><strong>Fix: multipart forms (file uploads) now pass CSRF with a <code>_csrf_token</code> field.</strong> Posting an action as <code>multipart/form-data</code> with a correct <code>_csrf_token</code> field was rejected with <code>403 CSRF token missing</code> — the middleware only read urlencoded bodies and the <code>X-CSRF-Token</code> header, so a progressive-enhancement <code>&lt;Form&gt;</code> containing a file input was broken before hydration. The middleware now stream-parses a multipart body just far enough to obtain <code>_csrf_token</code> (which <code>&lt;Form&gt;</code> renders before all other fields) and then replays the consumed bytes downstream untouched — file parts are never buffered by the CSRF layer, so uploads of any size work. The pre-token scan is capped at 1 MiB; past the cap (or when the field is absent) the request is rejected with a <code>403</code> that says exactly what to do: send the token in the <code>X-CSRF-Token</code> header, or place <code>_csrf_token</code> before the file fields. <code>missing</code> vs <code>mismatch</code> errors are now accurate on every path, including a new <code>CSRF cookie missing</code> message when a token is submitted without its cookie half. See <a href=\"/docs/guides/security#form-bodies-and-uploads\">Security → Form bodies and uploads</a>.</p>\n</li>\n<li><p><strong><code>pyxle dev</code> output is clean and readable by default, with <code>--verbose</code> for the firehose.</strong> The dev console used to dump Vite&#39;s raw line-by-line stdout and a wall of internal build chatter, burying the one thing you need — the URL. <code>pyxle dev</code> now prints a curated startup summary (the local URL, the Vite URL, the route count, and the total &quot;ready in X ms&quot;), a concise one-line notice per incremental rebuild (<code>Rebuilt … in X ms</code>), and any warnings or errors — nothing else. The raw <code>[vite] …</code> firehose and the step-by-step internals are hidden behind <code>--verbose</code> (or the global <code>pyxle -v dev</code>), which restores the full output for troubleshooting. Genuine signal — errors, warnings, the URLs, and rebuild success/failure — is always shown. <code>pyxle serve</code> (production) output is unchanged. See <a href=\"/docs/reference/cli#pyxle-dev\">CLI → pyxle dev</a>.</p>\n</li>\n<li><p><strong>Server-side logs now stream to the browser console during <code>pyxle dev</code>.</strong> Your server-side <code>logging</code> output (from loaders, actions, and your own modules) is forwarded over the dev overlay&#39;s WebSocket to the browser devtools console, prefixed <code>[pyxle:server]</code> and mapped to the matching <code>console</code> method (<code>info</code>→<code>console.info</code>, <code>warning</code>→<code>console.warn</code>, <code>error</code>→<code>console.error</code>) — so you can follow server logs without leaving the browser. By default only <code>INFO</code>+ from your own loggers is forwarded; <code>--verbose</code> also forwards <code>DEBUG</code> records and the framework&#39;s internal loggers. The bridge is bounded and non-blocking (it never stalls the event loop, drops records when no tab is connected or a send fails, guards against re-entrancy, and throttles bursts), and is strictly development-only — it never runs under <code>pyxle serve</code> and never ships in the production bundle. See <a href=\"/docs/reference/cli#pyxle-dev\">CLI → pyxle dev</a> and <a href=\"/docs/architecture/dev-server#the-error-overlay\">Architecture → the error overlay</a>.</p>\n</li>\n<li><p><strong><code>public/</code> changes are picked up on refresh instead of triggering a rebuild (matching Next.js).</strong> Editing, adding, or deleting a file under <code>public/</code> no longer runs a build pass or forces a browser reload. Static assets are served live from disk, so an edited file shows up on the next request/refresh, and dev now serves public assets with a revalidating <code>no-cache</code> header (previously a one-hour cache masked edits until it expired). The dev server keeps a lightweight watch on <code>public/</code> for the sole purpose of keeping its static-file index current, so a <strong>newly added or removed</strong> asset becomes (un)discoverable without restarting <code>pyxle dev</code>. This also removes the root cause of the Tailwind reload loop: with <code>tailwind.config.*</code> and no PostCSS config, the standalone <code>tailwindcss --watch</code> compiler writes <code>public/styles/tailwind.css</code> into the watched tree — that write used to trigger a rebuild that let Tailwind re-emit the CSS, a self-sustaining loop (about once a second, most visible on Linux). Because <code>public/</code> writes no longer rebuild, the loop cannot occur; genuine CSS edits still hot-reload through the <code>pages/styles/*.css</code> source path. The rebuild watch (<code>pages/</code> and any <code>dev.watch</code> extras) still ignores generated build output — <code>.pyxle-build</code>/<code>__pycache__</code> trees, <code>*.pyc</code> bytecode, and <code>*.db</code>/<code>*.db-wal</code>/<code>*.db-shm</code> journals. See <a href=\"/docs/guides/styling#legacy-tailwind-v3-projects\">Styling → Legacy Tailwind v3 projects</a> and <a href=\"/docs/architecture/dev-server#the-watcher\">Architecture → the watcher</a>.</p>\n</li>\n<li><p><strong>Watch extra directories for hot reload with <code>dev.watch</code>, and ignore extra paths with <code>dev.ignore</code>.</strong> Pyxle watches <code>pages/</code> for hot reload, but a shared Python module imported from outside it (e.g. <code>lib/</code> or <code>components/</code>) would not trigger a rebuild. A new <code>dev</code> config block closes that gap: <code>dev.watch</code> lists extra project-relative directories to also watch (a change under one runs the normal rebuild + module reload), and <code>dev.ignore</code> lists extra glob patterns to skip. <code>dev.ignore</code> is <strong>additive</strong> to the built-in generated-output ignores — those are load-bearing and always stay in force, so a user list can only add to them, never clear them. <code>dev.watch</code> entries must resolve inside the project root; a traversal path (<code>../…</code>) or absolute path is rejected at config load. Both default empty, and both are dev-only (ignored by <code>pyxle serve</code>). See <a href=\"/docs/reference/configuration#development\">Configuration → Development</a>.</p>\n</li>\n</ul>\n<h3 id=\"documentation\">Documentation</h3><ul>\n<li><strong>The flagship WebSockets example no longer crashes SSR when copy-pasted.</strong> The first example in the <a href=\"/docs/guides/websockets\">WebSockets guide</a> passed <code>window.location.pathname</code> to <code>useWebSocket()</code> — evaluated during the server render, where <code>window</code> doesn&#39;t exist, so following the guide verbatim 500&#39;d the page. The example now derives the socket path from the route&#39;s loader data (the loader returns the <code>room</code> segment; the component templates it into the same path the page was served from), and a callout explains the rule: the hook never <em>connects</em> during SSR, but its arguments are evaluated there — build paths from props/params, not browser globals.</li>\n<li><strong>Fixed a broken <code>@action</code> example in the <a href=\"/docs/plugins/pyxle-db\">pyxle-db docs</a>.</strong> The request-scoped-access example inserted an undefined <code>title</code> instead of reading the request body; it now uses <code>body = await request.json()</code> (<code>request.json</code> is an async method — subscripting it without awaiting fails at runtime), matching the <a href=\"/docs/core-concepts/server-actions\">Server Actions</a> page.</li>\n<li><strong>Documented CSRF on the pyxle-auth credential endpoints.</strong> <code>POST /auth/login</code> / <code>/auth/signup</code> / <code>/auth/logout</code> are CSRF-protected like every state-changing POST, so a raw curl per the old docs got <code>403</code> with no explanation. A new <a href=\"/docs/plugins/pyxle-auth#csrf\">CSRF subsection</a> states this, explains the double-submit dance (any GET issues the token cookie; echo it via the <code>X-CSRF-Token</code> header), notes that <code>useAuth()</code> does it automatically, and shows a working curl sequence.</li>\n<li><strong>Documented where pyxle-auth accounts live.</strong> A new <a href=\"/docs/plugins/pyxle-auth#where-accounts-live\">Where accounts live</a> section covers the SQLite database auto-created at <code>data/app.db</code> on first run (no manual migration step) and how to point at a production database via pyxle-db&#39;s <code>url</code> setting; the <a href=\"/docs/plugins/pyxle-db#plugin-settings\">pyxle-db settings docs</a> now also state that the file and its directory are created automatically.</li>\n<li><strong>Honest scope note on <code>pyxle check</code>.</strong> The <a href=\"/docs/reference/cli#pyxle-check\">CLI reference</a> now spells out that a green check validates syntax and semantics but does not prove a page renders — runtime-only mistakes (e.g. a component reading a loader key that doesn&#39;t exist) surface when the page loads, not in <code>check</code>. It also notes that as of 0.7.0 the JSX level works out of the box, since pyxle-langkit ships with the framework (previously the <code>[langkit]</code> extra).</li>\n<li><strong>Prerequisites up front.</strong> The <a href=\"/docs/getting-started/introduction\">Introduction</a> now says on page one that Pyxle needs Python 3.10+ (3.12 recommended) and Node.js 20.19+ with npm, matching <a href=\"/docs/getting-started/installation\">Installation</a>.</li>\n<li><strong><code>Accept: text/markdown</code> negotiation now follows RFC 9110.</strong> The check used to be a substring match on the raw <code>Accept</code> header, which ignored q-values entirely — <code>text/html, text/markdown;q=0</code> served Markdown even though <code>q=0</code> means <em>not acceptable</em>, and <code>text/html;q=0.9, text/markdown;q=0.8</code> served Markdown even though HTML was preferred. The header is now properly parsed: media types match by exact type/subtype token (no substrings — <code>text/markdownish</code> no longer counts), q-values are honoured (<code>q=0</code> excludes; default <code>1.0</code>, clamped to <code>0..1</code>), wildcards like <code>*/*</code> never select Markdown, ties go to Markdown, and malformed headers never raise — they fall back to HTML. Since negotiated responses carry <code>Vary: Accept</code>, shared caches now key on the correct variant. New public helper: <code>pyxle.devserver.llms.markdown_is_acceptable(accept)</code>. See <a href=\"/docs/guides/llms#negotiation-rules\">AI accessibility → Negotiation rules</a>.</li>\n<li><strong>The generated <code>/llms.txt</code> no longer advertises Markdown that isn&#39;t there.</strong> The default index used to emit relative links and to link every concrete page&#39;s <code>.md</code> URL — including pages whose <code>.md</code> URL just <code>307</code>-redirects to HTML. It now emits <strong>absolute URLs</strong> derived from the request&#39;s scheme and host (proxy headers respected), links <code>.md</code> only for pages whose Markdown actually resolves (a co-located <code>.md</code>, a <code>to_markdown</code> handler, an ancestor <code>llms.py</code> — or every page when <code>autoConvert</code> is on), and lists the rest at their canonical HTML URL. Custom <code>llms_txt</code> hooks are unaffected; <code>ctx.render_default()</code> returns the improved index. See <a href=\"/docs/guides/llms#the-llmstxt-index\">AI accessibility → The /llms.txt index</a>.</li>\n<li><strong>Converted Markdown keeps agents on the Markdown channel</strong> (breaking behavior change). <code>autoConvert</code> output — and the public <code>html_to_markdown()</code> helper — now rewrites internal page links to their <code>.md</code> renditions: <code>[About](/about)</code> becomes <code>[About](/about.md)</code>, preserving query strings and fragments (<code>/about?x=1#y</code> → <code>/about.md?x=1#y</code>). External URLs, <code>mailto:</code>/<code>tel:</code> links, <code>/api/</code> routes, asset paths with a file extension, and links already ending in <code>.md</code> are untouched, and <code>wrap_markdown</code> hooks receive the rewritten Markdown. <strong>Breaking:</strong> <code>html_to_markdown()</code> rewrites by default — pass <code>rewrite_links=False</code> for the old verbatim behavior. See <a href=\"/docs/guides/llms#autoconvert-the-lossy-fallback\">AI accessibility → autoConvert</a>.</li>\n<li><strong>Fix: a burst of rapid file saves can no longer kill the dev server.</strong> Two dev-server robustness fixes land together. First, build passes are now serialized and <code>meta.json</code> is written atomically and only when its content changed — previously a debounce-timer race could run two rebuilds at once, and one pass reading the build metadata mid-write misdiagnosed it as a schema mismatch and wiped the whole <code>.pyxle-build</code> cache, deleting and recreating <code>vite.config.js</code>. Vite answers a config change with a full restart, and a restart racing the next rebuild could die (<code>[vite] process exited</code>), leaving nothing listening until a manual restart. Second, the Vite subprocess is now properly supervised: <strong>any</strong> exit the dev server didn&#39;t initiate — including a &quot;clean&quot; exit code 0 — logs a clear warning and relaunches Vite with a short exponential backoff (0.5s, doubling) and a bounded budget (3 consecutive attempts). If relaunching keeps failing, a structured <code>ViteSupervisionError</code> is surfaced loudly in the terminal instead of a silently dead asset server.</li>\n<li><strong>Fix: touching <code>request.state.&lt;name&gt;</code> without the providing plugin now explains itself.</strong> A loader or <code>@action</code> that reads a <code>request.state</code> attribute nothing populated (most commonly <code>request.state.db</code> without the pyxle-db plugin) used to 500 with a bare <code>AttributeError: &#39;State&#39; object has no attribute &#39;db&#39;</code> and a raw traceback that pointed nowhere. The framework now detects exactly this case and reports it as a structured error with guidance: state attributes are provided by plugins or middleware — e.g. <code>request.state.db</code> requires the pyxle-db plugin in <code>pyxle.config.json</code> (<code>&quot;plugins&quot;: [&quot;pyxle-db&quot;]</code>). In dev the message appears in the error overlay, the action&#39;s JSON error, and the server log (with the original traceback chained); in production the response stays generic, as always. Any other <code>AttributeError</code> from your code flows through unchanged.</li>\n<li><strong>Actionable SSR error when a component touches a browser global.</strong> A component that evaluates <code>window</code>, <code>document</code>, <code>navigator</code>, <code>localStorage</code>, <code>sessionStorage</code>, <code>location</code>, <code>matchMedia</code>, or <code>requestAnimationFrame</code> at render scope used to fail SSR with a bare <code>window is not defined</code> and a traceback of framework frames — naming the route but never your file, with no hint at the fix. Dev mode (error page, overlay, log) now explains that browser globals don&#39;t exist during server-side rendering, names the page&#39;s <code>.pyxl</code> source file, and gives the remedy: move the browser-only code into a <code>useEffect</code> hook or an event handler, or render the subtree with <code>&lt;ClientOnly&gt;</code> — see the <a href=\"/docs/guides/client-components\">Client Components guide</a>. Production responses stay generic (no file paths or internals in the body); the enriched explanation goes to the server log. <code>ReferenceError</code>s on other names, and all other render errors, flow through exactly as before.</li>\n</ul>\n<h2 id=\"061-2026-07-01\">0.6.1 — 2026-07-01</h2><p>A sharper <code>pyxle check</code> — the edit → check → fix loop now catches classes of mistake it used to wave through.</p>\n<ul>\n<li><strong><code>pyxle check</code> gained a semantic layer.</strong> Beyond Python syntax, it now runs pyflakes over the Python section and reports <strong>undefined names</strong> — e.g. a handler that <code>raise</code>s a symbol it never imported — plus unused imports and redefinitions. Compiler-injected runtime names are recognized, so the idiomatic decorators and error classes never read as undefined.</li>\n<li><strong>Duplicate <code>export default</code> is now caught.</strong> <code>@babel/parser</code> accepts two default exports even though the build (esbuild) rejects them — so a build-breaking mistake used to pass <code>check</code> and only fail later, pointing into a generated path. It&#39;s now flagged at check time, at the real source line.</li>\n<li><strong>JSX error lines are now accurate.</strong> A JSX syntax error was always reported at the first line of the JSX section; it now points at the actual line. Babel&#39;s misleading &quot;Unterminated regular expression&quot; (its lexer&#39;s guess for an unclosed <code>{ }</code>) also carries a plain-language hint about the real cause.</li>\n<li><strong><code>LoaderError</code> and <code>invalidate_routes</code> are now auto-injected</strong>, matching <code>ActionError</code> / <code>ValidationActionError</code>. <code>raise LoaderError(...)</code> in a loader just works with no import — closing an inconsistency where the action-side equivalent already did. A name you import or define yourself still takes precedence. See <a href=\"/docs/reference/runtime-api\">Runtime API</a>.</li>\n<li><strong><code>pyxle install --break-system-packages</code>.</strong> For externally-managed (PEP 668) Python environments without a virtualenv, <code>pyxle install</code> now offers the flag and points to it in the no-venv warning. See <a href=\"/docs/reference/cli#pyxle-install\">CLI → pyxle install</a>.</li>\n</ul>\n<h2 id=\"060-2026-07-01\">0.6.0 — 2026-07-01</h2><ul>\n<li><strong>AI accessibility — serve your app as markdown, plus <code>llms.txt</code>.</strong> A new opt-in <code>llms</code> config block makes every Pyxle app legible to AI assistants and coding agents. Enable it (<code>&quot;llms&quot;: true</code>) and the framework serves a clean <strong>markdown</strong> rendition of each page at its URL with <code>.md</code> appended (<code>/docs/routing.md</code>), returns markdown from the same URL to requests that send <code>Accept: text/markdown</code> (browsers are unaffected), serves an <strong><code>/llms.txt</code></strong> index, and advertises it with <code>Link</code>/<code>X-Llms-Txt</code> discovery headers. A page&#39;s markdown is resolved from your project — a co-located <code>&lt;page&gt;.md</code> file, a <code>to_markdown</code> handler in the page&#39;s server module, or a <code>to_markdown</code> in the nearest ancestor <code>llms.py</code> (which scopes to a whole route subtree; <code>pages/llms.py</code> is app-wide) — with an opt-in <code>autoConvert</code> HTML→markdown fallback and a redirect-to-the-page fallback when nothing resolves. <code>/llms.txt</code> comes from a static <code>public/llms.txt</code>, a <code>llms_txt</code> hook in <code>pages/llms.py</code>, or a generated index, and a <code>wrap_markdown</code> hook in <code>pages/llms.py</code> can frame every <code>.md</code> response with a header/footer (e.g. agent navigation hints). Off by default; adds nothing to the page hot path. See the <a href=\"/docs/guides/llms\">AI accessibility guide</a> and <a href=\"/docs/reference/configuration#ai-accessibility\">Configuration → AI accessibility</a>.</li>\n</ul>\n<h2 id=\"050-2026-06-18\">0.5.0 — 2026-06-18</h2><p>The depth release: caching/SSG/ISR, streaming SSR, realtime (WebSockets + a\ncross-worker Redis broker), Pydantic-validated actions, observability,\nbackground work, image optimization, and multi-worker serving — built,\ndocumented, and dogfooded on pyxle.dev. Still <code>0.x</code> (beta); the two behavior\nchanges below are called out explicitly.</p>\n<ul>\n<li><strong>Cross-worker realtime — a Redis pub/sub broker.</strong> WebSocket channels (<code>pyxle.realtime.channel</code> / <code>room.publish</code>) now span worker processes and machines. The default <code>InProcessBroker</code> lives in one process, so under <code>pyxle serve --workers N</code> a client on one worker never saw messages published on another. Set <code>PYXLE_REALTIME_BROKER=redis</code> (with the <code>pyxle-framework[redis]</code> extra and <code>PYXLE_REALTIME_REDIS_URL</code>) and every worker relays through Redis: a message published on any worker reaches every subscriber on every worker, delivered once per connection. <strong>No code change</strong> — your handler&#39;s <code>channel(ws, &quot;room&quot;)</code> / <code>room.publish(...)</code> are identical; only the broker behind <code>app.state.pyxle_broker</code> changes. The connection is pinged at startup (a bad URL fails loudly) and the listener reconnects automatically. The in-process broker remains the default and is correct for <code>pyxle dev</code> and single-worker serve. See <a href=\"/docs/guides/websockets#cross-worker-realtime-with-redis\">WebSockets → Cross-worker realtime with Redis</a>.</li>\n<li><strong>Fix: streaming SSR now survives production gzip.</strong> Production serves enable gzip, and Starlette&#39;s stock <code>GZipMiddleware</code> buffers the whole streamed response (it never flushes the compressor between chunks), so a streamed <code>&lt;Suspense&gt;</code> page arrived all at once instead of shell-first — defeating streaming for every app, since browsers always send <code>Accept-Encoding: gzip</code>. Pyxle now ships a streaming-aware <code>StreamingGZipMiddleware</code> (from <code>pyxle.middleware</code>) that flushes the compressor after every chunk, so the shell&#39;s compressed bytes reach the browser immediately. Compression and streaming are both preserved under <code>pyxle serve</code>, no configuration needed. See <a href=\"/docs/guides/streaming#streaming-survives-gzip-in-production\">Streaming SSR → Streaming survives gzip</a>.</li>\n<li><strong>Fix: dynamic pages are no longer client-nav-cached with stale data.</strong> The client navigation cache previously held every route&#39;s loader payload for the default window (2 minutes), so navigating away from and back to a <strong>dynamic</strong> page (a <code>@server</code> loader with no declared cache lifetime — e.g. a live feed) showed stale data until a hard refresh. The navigation-cache TTL now mirrors the server&#39;s real cacheability: a dynamic page (rendered <code>private, no-cache</code>) is <strong>not</strong> nav-cached (TTL <code>0</code>), so back/forward always refetches and a just-made mutation shows immediately. Pages opt into nav-caching via a <code>cache</code> entry; static, loader-less pages keep the 2-minute default. See <a href=\"/docs/guides/caching#client-navigation-cache\">Caching → Client navigation cache</a> and <a href=\"/docs/reference/client-api#navigation-cache-ttl\">Navigation cache TTL</a>.</li>\n<li><strong>Fix: <code>pyxle build --static</code> pre-renders pages whose loaders use a plugin.</strong> Static pre-rendering ran page and layout loaders without an active plugin context, so any app that touched a plugin service in a loader — most commonly a layout loader calling <code>pyxle-db</code>&#39;s <code>get_database()</code> — crashed with <code>No active plugin context</code> and pre-rendered <strong>nothing</strong> (0 pages). The static builder now stands up the same plugin context a request sees (loads the configured plugins and runs their startup, opening DB connections) around the pre-render, then tears it down — so plugin-backed loaders run at build time exactly as they do at request time, which is how static generation is meant to work (read your data source at build). See <a href=\"/docs/guides/caching#static-pre-rendering-pyxle-build-static\">Caching → Static pre-rendering</a>.</li>\n<li><strong>Changed (behavior): stricter validation of nested config blocks.</strong> The <code>cors</code>, <code>csrf</code>, <code>observability</code>, and <code>routeMiddleware</code> blocks (and <code>styling</code> / the <code>starlette</code> &amp; <code>vite</code> network blocks) now <strong>reject unknown keys at boot</strong> instead of silently ignoring them. A typo&#39;d or mis-cased nested key — e.g. <code>csrf.cookieSamesite</code> instead of <code>cookieSameSite</code> — that booted before now fails loudly with a clear <code>Unknown keys in &#39;&lt;block&gt;&#39; block</code> error. This is a deliberate fail-loud fix: a silently-dropped key could leave a security-relevant default in place. Fix: correct the key name. See <a href=\"/docs/reference/configuration\">Configuration</a>.</li>\n<li><strong>Fix: <code>error.pyxl</code> no longer leaks internal error details in production.</strong> When a loader or SSR render raised a non-author exception, the nearest <code>error.pyxl</code> boundary received the raw exception message (and the exception class name) as props — potentially a Node SSR stack trace or a filesystem path — which it could render to end users. In production the boundary now gets a generic <code>&quot;An unexpected error occurred.&quot;</code> and a sanitized <code>ServerError</code> type; author-raised <code>LoaderError</code> / <code>ActionError</code> messages still pass through (they&#39;re meant to be shown). Dev keeps the real (secret-redacted) message. See <a href=\"/docs/guides/error-handling\">Error Handling</a>.</li>\n<li><strong>Fix: <code>import.meta.env.PYXLE_PUBLIC_*</code> is now substituted during SSR.</strong> The Vite <code>define</code> for public env vars was applied only to the client bundle, so at server render the expression was <code>undefined</code> — any public env var rendered into the initial HTML caused a hydration mismatch / blank first paint. The SSR esbuild step now gets the same <code>define</code> (same keys, same quoting), so server and client agree. See <a href=\"/docs/guides/environment-variables\">Environment variables → Client-side variables</a>.</li>\n<li><strong>Fix: scaffold <code>.gitignore</code> no longer ignores <code>.env</code>.</strong> A fresh <code>pyxle init</code> git-ignored <code>.env</code> even though the env-vars doc tells you to commit <code>.env</code> / <code>.env.development</code> / <code>.env.production</code> and ignore only the <code>.local</code> variants. The scaffold now matches the doc.</li>\n<li><strong>Fix: <code>&lt;Image&gt;</code> emits a lowercase <code>fetchpriority</code> attribute.</strong> The <code>priority</code> image rendered camelCase <code>fetchPriority</code>, which React 18.3.1 rejects with an unknown-prop warning on every such image. It now emits the valid lowercase HTML attribute.</li>\n<li><strong>Fix: production gzip no longer prints <code>I/O operation on closed file</code>.</strong> <code>StreamingGZipMiddleware</code> now closes its <code>GzipFile</code> + buffer deterministically on every path (small/already-encoded responses, and streams interrupted mid-flight), so the GC no longer finalizes an open compressor and the noisy <code>Exception ignored while finalizing file … I/O operation on closed file</code> stderr messages are gone.</li>\n<li><strong>Fix: the scaffolded <code>jsconfig.json</code> no longer trips TypeScript&#39;s <code>baseUrl</code> deprecation.</strong> VS Code (TypeScript 6+) flags <code>baseUrl</code> as deprecated — slated for removal in TS 7 — so every fresh project opened with a warning on a file Pyxle generated. The scaffold now declares the import alias with tsconfig-relative <code>paths</code> only (<code>&quot;@/*&quot;: [&quot;./*&quot;]</code>), which resolves identically without <code>baseUrl</code>; editors, Vite, the SSR runtime, and shadcn/ui all read the alias the same way as before.</li>\n<li><strong>Fix: <code>pyxle typecheck</code> works out of the box on current TypeScript.</strong> The generated <code>tsconfig.json</code> used <code>moduleResolution: &quot;node10&quot;</code> and <code>baseUrl</code>, both deprecated in TypeScript 7.0, so <code>tsc</code> failed with TS5107/TS5101 on a new-enough TypeScript. It now uses <code>&quot;bundler&quot;</code> resolution (correct for the Vite/esbuild pipeline) with tsconfig-relative <code>paths</code> and no <code>baseUrl</code>.</li>\n<li><strong>Startup warning when a <code>BaseHTTPMiddleware</code> is paired with streaming routes.</strong> A custom middleware subclassing Starlette&#39;s <code>BaseHTTPMiddleware</code> buffers the whole response, which breaks streaming SSR (a <code>&lt;Suspense&gt;</code> page would arrive all at once / raise <code>No response returned</code> on a mid-stream disconnect). Pyxle now logs a clear warning at startup naming the offending middleware class and pointing to the pure-ASGI pattern. See <a href=\"/docs/guides/middleware\">Middleware</a>.</li>\n<li><strong>Clear compile-time error for TypeScript syntax in a client block.</strong> A <code>.pyxl</code> client block is plain JSX — type annotations (<code>const x: number</code>), <code>interface</code>, generics, and <code>as</code> casts aren&#39;t supported there. Previously such syntax slipped past the compiler (Babel parses it) and failed later in esbuild/Vite with a confusing message pointing into a generated <code>.pyxle-build</code> path. The compiler now detects it up front and raises a clear error pointing at your <code>.pyxl</code> source line: &quot;TypeScript syntax (…) isn&#39;t supported in a .pyxl client block yet — keep the client half plain JSX.&quot; Detection is AST-based (any TS-only node), so valid JSX — ternaries, object literals, the <code>as</code> prop — is never flagged. See <a href=\"/docs/guides/typescript\">TypeScript</a>.</li>\n<li><strong>New guides: TypeScript and migrating from Flask/Django.</strong> <a href=\"/docs/guides/typescript\">TypeScript</a> documents Pyxle&#39;s consumption-not-authoring model — the generated <code>tsconfig.json</code> and <code>.d.ts</code> declarations for every <code>pyxle/client</code> hook/component, the <code>pyxle typecheck</code> gate, and the honest limits (no <code>.tsx</code> authoring or per-page loader types yet). <a href=\"/docs/guides/migration-from-flask-django\">Migrating from Flask or Django</a> maps the concepts you know (routes, views, templates, forms, the ORM, middleware, config) to their Pyxle equivalents, with a frank list of what has no direct equivalent (admin, <code>url_for</code>, flash messages, a core ORM).</li>\n<li><strong><code>error.pyxl</code> is now a client-side error boundary too.</strong> The server already renders the nearest <code>error.pyxl</code> when a loader or the initial render fails; now that same boundary keeps working in the browser. If a component throws while re-rendering — after a state update, during a client-side navigation, or on a hydration fault — Pyxle catches it and renders the nearest <code>error.pyxl</code> in place, instead of React unmounting the page to a blank screen. The boundary receives the <strong>same</strong> <code>error</code> prop (<code>message</code>, <code>statusCode</code>, <code>type</code>) on both sides, so one <code>error.pyxl</code> renders identically wherever the fault happened. It is transparent until something throws (so hydration is byte-for-byte unchanged) and resets on the next navigation; the nearest <code>error.pyxl</code> is resolved per route (closest-ancestor walk-up) and threaded to the client like the streaming <code>loading.pyxl</code> boundary. In <code>pyxle dev</code> the error overlay still surfaces the full stack on top; in production the boundary is the user-facing fallback. This catches <em>render</em> faults — an error in an event handler or an <code>await</code> is handled where it occurs (a <code>try/catch</code>, or an <code>ActionError</code> from <code>useAction</code>). See <a href=\"/docs/guides/error-handling#client-side-errors\">Error Handling → Client-side errors</a>.</li>\n<li><strong>Built-in rate limiting — <code>pyxle.middleware.RateLimitMiddleware</code>.</strong> A dependency-free <strong>token-bucket</strong> limiter, enabled from <code>pyxle.config.json</code> with no code: <code>{&quot;rateLimit&quot;: {&quot;requests&quot;: 100, &quot;window&quot;: 60}}</code>. Each client (keyed by remote IP, or the first <code>X-Forwarded-For</code> hop when <code>trustForwardedFor</code> is set behind a trusted proxy) gets a bucket of <code>requests</code> tokens that refills at <code>requests / window</code> per second — so a brief burst is allowed while the sustained rate is bounded, friendlier than a hard fixed window. Over-limit requests get <code>429 Too Many Requests</code> with a <code>Retry-After</code> header. <code>exemptPaths</code> skips health checks and metrics scrapes (segment-boundary matching, like <code>csrf.exemptPaths</code>). The limiter sits just inside request observability, so a throttled request still gets a correlation id and is counted, but is rejected before CSRF, static serving, or the handler do any work; the bucket store is bounded by an LRU cap so a flood of unique clients can&#39;t grow memory without bound. <strong>Multi-worker caveat:</strong> the store is in-memory and per-process, so under <code>pyxle serve --workers N</code> the effective global cap is <code>N × requests</code> — for one shared limit, rate-limit at your reverse proxy. Off by default (<code>requests: 0</code>). See <a href=\"/docs/guides/middleware#rate-limiting\">Middleware → Rate limiting</a> and <a href=\"/docs/guides/security#rate-limiting\">Security → Rate limiting</a>.</li>\n<li><strong>Route policies now apply to <code>@action</code> endpoints.</strong> Previously, a route hook registered under <code>routeMiddleware</code> wrapped page and API routes but <strong>not</strong> <code>@action</code> calls — so an auth or rate-limit policy could be silently bypassed by hitting the action endpoint directly. Actions now run through the same route-hook pipeline; register action policies under the new <code>routeMiddleware.actions</code> list. Because an action replies with JSON, an action hook should reject with a JSON <code>401</code>/<code>403</code> rather than an HTML redirect (which is why actions get their own list instead of inheriting the page&#39;s hooks). See <a href=\"/docs/guides/middleware#route-level-hooks\">Middleware → Route-level hooks</a>.</li>\n<li><strong><code>pyxle serve --workers 0</code> auto-detects the core count.</strong> Multi-core serving (<code>pyxle serve --workers N</code>, each worker an independent process sharing one socket) already shipped; now <code>--workers 0</code> sizes the worker count to the host&#39;s CPU cores, so you don&#39;t hard-code it per machine. The <a href=\"/docs/guides/deployment#multi-core-worker-processes\">Deployment guide</a> gains a consolidated <strong>per-worker state</strong> table (which features are per-process — page cache, WebSocket pub/sub, metrics, background tasks — and the shared-backend escape hatch for each), <strong>rolling-deploy / graceful-restart</strong> guidance (blue-green via <code>/readyz</code>, systemd, and the gunicorn-preload option), and the rationale for the share-nothing, per-worker-SSR-pool design.</li>\n<li><strong>Build optimization: responsive <code>&lt;Image&gt;</code>, preload hints, and <code>--analyze</code>.</strong> <code>&lt;Image&gt;</code> (from <code>pyxle/client</code>) is now an optimized image on par with Next.js&#39;s for everything that doesn&#39;t need a server-side optimizer: pass a <code>loader</code> (a CDN like Cloudinary/imgix, or a build plugin) and it emits a responsive <code>srcset</code> across a device-size ladder; it also gains <code>fill</code> mode, <code>sizes</code>, <code>quality</code>, <code>objectFit</code>, and <code>priority</code> → <code>fetchpriority=&quot;high&quot;</code> for the LCP image — plus the existing blur placeholder, <code>fallbackSrc</code>, lazy-loading, and layout-shift prevention. Without a loader it stays a clean <code>&lt;img&gt;</code> (no fake srcset that re-downloads the full image — resizing needs a real backend, which stays opt-in). Separately, the SSR shell now injects <code>&lt;link rel=&quot;modulepreload&quot;&gt;</code> for each page&#39;s entry and the chunks it imports (from the build manifest&#39;s import graph), so they download in parallel with HTML parsing; <code>pyxle build --analyze</code> prints a dependency-free JS/CSS bundle-size report (raw + gzip); and the generated Vite config carries an explicit <code>build</code> block (faster builds — Vite&#39;s slow gzip-size reporting is off since <code>--analyze</code> covers it). JS/CSS minification, tree-shaking, and code-splitting were already on via Vite&#39;s production defaults. See <a href=\"/docs/guides/build-optimization\">Build Optimization</a>.</li>\n<li><strong>Background tasks &amp; deferred work.</strong> Two ways to run work off the request&#39;s critical path. Inside an <code>@action</code>, <code>request.state.background.add_task(fn, *args)</code> (a Starlette <code>BackgroundTasks</code>) runs after the response is sent — ideal for &quot;send the welcome email after signup&quot;; returning <code>{&quot;background&quot;: [fn, *args]}</code> is shorthand for a single such task. For fire-and-forget work from anywhere (loaders, actions, API routes), the new <code>pyxle.tasks.enqueue(fn, *args)</code> schedules it on a small <strong>in-process</strong> worker pool — coroutine functions are awaited, plain callables run in a thread, a failing task is logged (never takes a worker down), and the queue is bounded (<code>TaskQueueFull</code> under overload). The queue starts/stops with the app. <strong>Multi-worker caveat:</strong> it&#39;s per-process (like the in-process WebSocket broker), so for durability or cross-worker/exactly-once semantics, hand off to a real job queue — the guide documents Celery, ARQ, and Dramatiq integration. See <a href=\"/docs/guides/background-tasks\">Background Tasks</a>.</li>\n<li><strong>OpenTelemetry tracing (opt-in).</strong> Emit OpenTelemetry spans for each request plus child spans for the SSR render, <code>@server</code> loaders, and <code>@action</code> calls, so a slow page surfaces as a drill-down trace. It&#39;s the heaviest integration, so it&#39;s a <strong>separate</strong> optional extra (<code>pip install &quot;pyxle-framework[observability-otel]&quot;</code>) and <strong>fully off</strong> by default — with the extra absent, spans are a zero-cost no-op (one boolean check), and the exporter endpoint comes from the standard <code>OTEL_EXPORTER_OTLP_ENDPOINT</code> env var. Sampling defaults to 5%. A base Pyxle install pulls in no OpenTelemetry packages; enabling <code>observability.otel</code> without the extra fails loudly at startup. See <a href=\"/docs/guides/observability#opentelemetry-tracing\">Observability → OpenTelemetry tracing</a>.</li>\n<li><strong><code>pyxle dev --dashboard</code> — a live terminal observability panel.</strong> Opt into a compact panel that refreshes every few seconds with request throughput and error rate, status-class breakdown, request/SSR-render/loader/action latency, and the page-cache hit ratio. It reads the same in-process metrics (so it&#39;s free on the request path), is <strong>dependency-free</strong> (no <code>rich</code>), and is dev-only — in production you&#39;d scrape the metrics endpoint instead. See <a href=\"/docs/guides/observability#dev-dashboard\">Observability → Dev dashboard</a>.</li>\n<li><strong>Structured access logging.</strong> Opt into one structured log line per request — <code>method</code>, <code>path</code>, <code>status</code>, <code>duration_ms</code>, and the request&#39;s correlation id — via <code>observability.accessLog</code>, in <code>&quot;console&quot;</code> (human-readable) or <code>&quot;json&quot;</code> format. The correlation id is bound into a context variable for the request&#39;s duration, so your own log calls pick it up too. Works with <strong>no new dependency</strong> (a stdlib JSON/console formatter); the optional <code>[observability]</code> extra adds <a href=\"https://www.structlog.org/\" target=\"_blank\" rel=\"noreferrer\">structlog</a> for richer rendering. Off by default so it won&#39;t disrupt an existing log pipeline. See <a href=\"/docs/guides/observability#structured-logging\">Observability → Structured logging</a>.</li>\n<li><strong>Observability: request IDs, timing, metrics, and richer health probes.</strong> Every request now gets a correlation id — surfaced as the <code>X-Request-Id</code> response header and on <code>request.state.request_id</code>, readable from any <code>@server</code> loader or <code>@action</code> — and its duration is measured, along with SSR-render, loader, action, and page-cache (hit/stale/miss) metrics. The request-id and timing are <strong>on by default</strong> (a <code>uuid4</code> and two <code>perf_counter</code> reads, no I/O, added at the outermost layer so even a CSRF rejection is tagged); an incoming <code>X-Request-Id</code> is <strong>not</strong> trusted unless you opt in (<code>observability.trustIncomingRequestId</code>), since echoing a client value into logs is a spoofing vector. Metrics are recorded into a bounded, allocation-cheap in-process registry and exposed — <strong>opt-in</strong> — at a Prometheus endpoint (<code>observability.metricsEndpoint</code>, optionally bearer-guarded); the page-cache hit ratio comes from the existing server cache. <code>/readyz</code> now runs <strong>dependency checks</strong> (today: the SSR worker pool has a live worker) and returns <code>503</code> until the server is genuinely ready, while <code>/healthz</code> stays a pure liveness probe. All configured under the new <a href=\"/docs/reference/configuration#observability\"><code>observability</code></a> block (<code>&quot;observability&quot;: false</code> disables request-id/timing); the helper <code>from pyxle.observability import get_request_id</code> reads the id anywhere. <strong>Multi-worker note:</strong> metrics are per-worker (each series carries a <code>worker</code> label) — aggregate at the scraper. Structured logging, a <code>pyxle dev</code> dashboard, and OpenTelemetry land next in Phase 2.7. See <a href=\"/docs/guides/observability\">Observability</a>.</li>\n<li><strong>Typed request validation for <code>@action</code> with Pydantic.</strong> An action can now declare a body parameter annotated with a <a href=\"https://docs.pydantic.dev/\" target=\"_blank\" rel=\"noreferrer\">Pydantic</a> model — <code>async def register(request, body: Signup)</code> — and Pyxle parses, coerces, and validates the request body before the action runs, passing in a fully-typed instance. No more hand-written <code>await request.json()</code> + key-by-key checks. On failure the action is <strong>not</strong> called and the client gets a <code>422</code> with a top-level <code>fields</code> map (field path → messages, dotted for nested models and indexed for list items: <code>address.zip</code>, <code>tags.0</code>). Both <code>useAction</code> and <code>&lt;Form&gt;</code> surface it — <code>result.fields</code> / the reactive <code>submit.fields</code>, and <code>&lt;Form onError={(message, fields) =&gt; …}&gt;</code> — so you can render messages next to each input. For checks Pydantic can&#39;t express (uniqueness, cross-field rules) raise the new <code>ValidationActionError</code>, which is auto-imported alongside <code>ActionError</code> and produces the same <code>422</code> shape. Because the body models are real Pydantic models, <code>pyxle openapi</code> generates an <a href=\"https://spec.openapis.org/oas/v3.1.0\" target=\"_blank\" rel=\"noreferrer\">OpenAPI 3.1</a> document of every action&#39;s request body straight from your code (<code>--out openapi.json</code>), derived from runtime introspection so it always matches what the dispatcher validates. Pydantic is an <strong>optional dependency</strong> — install the extra with <code>pip install &quot;pyxle-framework[pydantic]&quot;</code>; actions without a model parameter are unaffected, and an action that declares one without Pydantic installed fails with a clear error rather than silently skipping validation. The generated <code>useAction</code>/<code>Form</code> runtimes now also ship TypeScript declarations (<code>.d.ts</code>). See <a href=\"/docs/core-concepts/server-actions#validating-request-bodies-with-pydantic\">Server Actions → Validating request bodies</a>.</li>\n<li><strong>Fix: a server module that fails to import is no longer cached as a broken empty module.</strong> If a page&#39;s compiled server module raised during its first import (e.g. a bad top-level <code>import</code>), the half-initialised module was left in <code>sys.modules</code>, so every later request for that page silently got an <em>empty</em> module instead of re-raising the real error. Failed imports are now evicted, so the error surfaces consistently on each request.</li>\n<li><strong>WebSockets — page handlers, a client hook, and pub/sub.</strong> A page <code>.pyxl</code> can now export a module-scope <code>async def websocket(ws)</code> and Pyxle serves a WebSocket route at the page&#39;s path, alongside its HTTP render — the same file handles both protocols, and dynamic segments (<code>pages/chat/[room].pyxl</code>) resolve into <code>ws.path_params</code>. Detection is by convention (the function name, not a decorator), matching how <code>pages/api/**</code> modules already expose a <code>websocket</code> callable. The new <code>pyxle.realtime</code> module ships a small pub/sub layer: <code>async with channel(ws, &quot;room&quot;) as room: await room.publish(msg)</code> subscribes a connection to a named channel and broadcasts to everyone in it, with stale-connection pruning. Because Starlette runs HTTP middleware only for <code>http</code> scope, a WS handler gets <strong>no</strong> middleware — so <code>pyxle.realtime</code> also provides <code>authenticate_websocket(ws)</code> (resolve the pyxle-auth session from the handshake cookie) and <code>origin_allowed(ws, origins)</code> (the WS equivalent of CSRF). On the client, the generated <code>useWebSocket(path, options?)</code> hook gives <code>{ status, send, lastMessage, error }</code> with JSON parsing and exponential-backoff reconnect, and never opens a socket during SSR. <strong>Multi-worker caveat (called out honestly):</strong> the default in-process broker lives in one process, so under <code>pyxle serve --workers N</code> a message published on one worker isn&#39;t seen by clients on another — Pyxle logs a warning when it detects this, and the <code>Broker</code> protocol lets a Redis-backed broker drop in. See <a href=\"/docs/guides/websockets\">WebSockets</a>.</li>\n<li><strong>Streaming SSR for <code>&lt;Suspense&gt;</code> pages.</strong> A page that wraps a slow part in a React 18 <code>&lt;Suspense&gt;</code> boundary now streams: Pyxle flushes the shell (everything outside the boundary) immediately for a fast time-to-first-byte, then streams each boundary&#39;s content in as it resolves. It&#39;s opt-in and zero-config — the compiler detects <code>&lt;Suspense&gt;</code> and only those pages stream; every other page keeps the buffered render byte-for-byte. The page hydrates exactly as before (the browser hydrates the same component), so no app changes are needed beyond adding the boundary. Streaming applies to dynamic, per-request pages only: a publicly cacheable route (<code>{&quot;data&quot;, &quot;revalidate&quot;}</code> envelope, <code>CACHE</code> directive, or edge <code>cache</code> entry) still renders buffered so it can be stored and <code>ETag</code>&#39;d, and a streamed response is always <code>private, no-cache</code>. The document head is flushed before the component renders, so a streamed page uses its static <code>HEAD</code> (a runtime-rendered <code>&lt;Head&gt;</code> lands too late — buffered pages are unaffected). A render error before the first byte falls back to the nearest <code>error.pyxl</code> rather than emitting a partial document. Built on the Node worker&#39;s new multi-frame <code>renderToPipeableStream</code> transport. See <a href=\"/docs/guides/streaming\">Streaming SSR</a>.</li>\n<li><strong><code>loading.pyxl</code> route-level loading states.</strong> A <code>loading.pyxl</code> file declares a loading component that wraps the whole route in <code>&lt;Suspense fallback={&lt;Loading/&gt;}&gt;</code> — streamed as the shell on the server and applied identically on the client so hydration matches. The nearest <code>loading.pyxl</code> wins (directory walk-up, like <code>error.pyxl</code>), and it composes with a page&#39;s own inner <code>&lt;Suspense&gt;</code>. Honest caveat: because the <code>@server</code> loader runs before render, the fallback only appears when the <em>render</em> suspends (a child using <code>React.lazy</code> or a thrown promise; Pyxle ships React 18.3.1, which has no <code>React.use</code>) — a page whose data comes entirely from its loader never suspends and streams at once. See <a href=\"/docs/guides/streaming#route-level-loading-states-with-loadingpyxl\">Streaming SSR → Route-level loading states</a>.</li>\n<li><strong>Server-side page caching with incremental regeneration.</strong> A <code>@server</code> loader can now return a <code>{&quot;data&quot;: ..., &quot;revalidate&quot;: N}</code> envelope to cache its rendered HTML for <code>N</code> seconds — later requests are served from the cache without re-running the loader or the Node SSR render. A loader-less page can opt in the same way with a module-level <code>CACHE = {&quot;revalidate&quot;: N}</code> directive. And <code>pyxle build --static</code> pre-renders every loader-less, non-dynamic page to HTML at build time and warms the cache from it on startup, so the first request after a deploy is a hit — no cold render. Once the window passes, the stale page is served instantly while a single background re-render refreshes it (incremental static regeneration — no thundering herd). Purge a route on demand from an action with <code>await cache.invalidate(&quot;/path&quot;)</code> (or <code>cache.invalidate_all()</code>). Routes already declared in the <code>pyxle.config.json</code> edge <code>cache</code> block are server-cached automatically under the same TTL. Cached responses carry a strong <code>ETag</code> (so conditional requests get a <code>304</code>) and an <code>x-pyxle-cache: HIT|STALE|MISS</code> header. The cache is enabled for <code>pyxle serve</code> (off in <code>pyxle dev</code>) and stores to a bounded in-memory backend by default, or to local disk or a shared Redis via <code>PYXLE_PAGE_CACHE_BACKEND</code> (Redis needs <code>pip install &#39;pyxle-framework[redis]&#39;</code>). Only cache pages that render no per-user data — a cached render is shared with every visitor. See <a href=\"/docs/guides/caching\">Caching</a>.</li>\n</ul>\n<h2 id=\"045\">0.4.5</h2><ul>\n<li><strong>Signed cookies &amp; tokens — <code>sign_cookie</code> / <code>verify_cookie</code>.</strong> New stdlib-only helpers (importable from <code>pyxle</code>) attach a tamper-proof HMAC-SHA256 signature to any string — a session id, a &quot;remember me&quot; token, a stateless unsubscribe or password-reset link — and verify it later in constant time. The secret comes from <code>PYXLE_SECRET_KEY</code> (or an explicit <code>secret_key=</code>), and a <code>salt=</code> namespaces signatures so a token minted for one purpose can&#39;t be replayed for another. Signing without a secret raises <code>MissingSecretKeyError</code> rather than returning an unprotected value — it fails closed. See <a href=\"/docs/guides/security#signed-cookies-and-tokens\">Security → Signed cookies and tokens</a>.</li>\n<li><strong>Fix: <code>PYXLE_PUBLIC_*</code> client env vars now work in <code>pyxle dev</code> and no longer break <code>pyxle build</code>.</strong> The generated Vite <code>define</code> emitted each value as a bare expression, so a non-identifier value (an API URL, a <code>0x…</code> Turnstile key) was silently dropped in dev and crashed <code>vite build</code> with esbuild&#39;s &quot;Invalid define value&quot;. Values are now emitted as quoted JS string literals, so <code>import.meta.env.PYXLE_PUBLIC_*</code> resolves correctly in both dev and production builds. See <a href=\"/docs/guides/environment-variables\">Environment variables → Client-side variables</a>.</li>\n<li><strong>Fix: web fonts and other CSS <code>url()</code> assets no longer 404 in <code>pyxle dev</code>.</strong> Vite rewrites <code>@font-face</code>/<code>url(...)</code> references to root-relative <code>/@fs/…</code> paths, which the browser resolved against Pyxle&#39;s origin instead of Vite&#39;s and 404&#39;d — so <code>@fontsource</code> <code>.woff2</code> files silently failed to load in development (production builds were unaffected, as their assets are hashed and served by Pyxle). Pyxle now sets Vite&#39;s <code>server.origin</code>, so dev assets load from Vite directly.</li>\n</ul>\n<h2 id=\"044\">0.4.4</h2><ul>\n<li><strong>Fix: cross-page hash links scroll to their anchor.</strong> Client-side navigation to <code>/page#section</code> changed the page but stayed pinned to the top (only the URL carried the hash); a full load scrolled correctly. The router now jumps to the top and then scrolls to the anchor as soon as the next page&#39;s DOM commits, matching native behaviour — including for unknown anchors, which stay at the top.</li>\n</ul>\n<h2 id=\"043\">0.4.3</h2><ul>\n<li><strong>Security: hardened HEAD sanitisation (XSS).</strong> Dynamic <code>HEAD</code> values/callables that interpolate loader data into raw head strings — the documented dynamic-meta-tags recipe — could previously inject markup because attribute values weren&#39;t escaped (a <code>&quot;</code> in a <code>&lt;meta content&gt;</code> broke out of the attribute). Head elements are now parsed and rebuilt from a strict tag allowlist with every attribute value HTML-escaped, <code>on*</code> handlers dropped, <code>javascript:</code>/<code>vbscript:</code>/<code>data:</code> URLs neutralised, <code>&lt;meta http-equiv=&quot;refresh&quot;&gt;</code> and non-head tags rejected, and any markup injected after a breakout discarded. Inline <code>&lt;script&gt;</code>/<code>&lt;style&gt;</code> head content remains supported as trusted author code. Self-closing head tags now serialise canonically as <code>/&gt;</code>.</li>\n<li><strong>Security: CSRF exempt paths match on segment boundaries.</strong> <code>csrf.exemptPaths</code> previously used a bare string prefix, so exempting <code>/api/webhooks</code> also exempted an adjacently-named sibling like <code>/api/webhooks-admin</code>. Exemptions now match a path exactly or at a <code>/</code> boundary.</li>\n<li><strong>Security: oversized no-JS form POSTs fail loud.</strong> A form-encoded POST whose body exceeded the CSRF buffer cap (1 MB) was silently truncated and replayed to your handler. Such requests now return <code>413</code> and ask for the token via the <code>X-CSRF-Token</code> header instead of corrupting the payload.</li>\n<li><strong>Fix: <code>&lt;Script&gt;</code>/<code>&lt;Image&gt;</code> boolean attributes written as strings.</strong> <code>defer=&quot;false&quot;</code>, <code>async=&quot;no&quot;</code>, <code>priority=&quot;0&quot;</code>, <code>lazy=&quot;false&quot;</code> and similar quoted-falsy forms were coerced to <code>True</code> (the opposite of the source). They now coerce correctly.</li>\n<li><strong>Fix: custom <code>csrf.cookieName</code> / <code>csrf.headerName</code> now reach the client runtime.</strong> Configuring either name made every action POST fail with <code>403</code> — the middleware set and validated the cookie under the configured name while <code>useAction</code>/<code>&lt;Form&gt;</code> kept reading a literal <code>pyxle-csrf</code> cookie and sending a literal <code>x-csrf-token</code> header. Non-default names are now injected into the document shell as <code>window.__PYXLE_CSRF_COOKIE__</code> / <code>window.__PYXLE_CSRF_HEADER__</code> and the client runtime resolves them with the defaults as fallback, in both dev and production builds.</li>\n<li><strong>Fix: no double loader run on hover-then-click navigation.</strong> When a <code>&lt;Link&gt;</code> was prefetched on hover/viewport and then clicked, the click raced a second request and ran the page&#39;s <code>@server</code> loader twice. The click now reuses the in-flight prefetch, and a navigation-sequence token discards a superseded prefetch so a slow one can&#39;t render a stale page over a newer click.</li>\n<li><strong>Multi-core serving: <code>pyxle serve --workers N</code>.</strong> Production serving can now run N independent server processes on one port (each with its own SSR pool and event loop) — throughput scales with cores, no load balancer or shared state to configure. See <a href=\"/docs/guides/deployment#multi-core-worker-processes\">Deployment → Multi-core (worker processes)</a>.</li>\n<li><strong>Sync API endpoints.</strong> A plain <code>def endpoint(request)</code> in <code>pages/api/</code> now runs in Starlette&#39;s threadpool instead of failing at request time — blocking database drivers and sync SDKs no longer require manual <code>asyncio.to_thread</code> wrapping. Sync <code>get</code>/<code>post</code>/… methods on <code>HTTPEndpoint</code> classes are threadpooled too, and <code>HTTPEndpoint</code> classes dispatch correctly alongside route hooks. See <a href=\"/docs/guides/api-routes#sync-endpoints-and-blocking-calls\">API Routes → Sync endpoints and blocking calls</a>.</li>\n<li><strong>In-memory static asset cache.</strong> When serving a production build, small static files (≤1 MB each, 32 MB total per process) are loaded into memory at startup and served without filesystem I/O or threadpool hops — several-fold higher static throughput. Conditional requests (<code>ETag</code>/<code>If-Modified-Since</code> → 304) and cache headers behave exactly as before; larger files keep streaming from disk.</li>\n</ul>\n<h2 id=\"042\">0.4.2</h2><ul>\n<li><strong>Live dev-server reconciliation.</strong> Editing a <code>.pyxl</code> in <code>pyxle dev</code> now applies route-<em>shape</em> changes without a restart — rename/add/remove a <code>@server</code> loader or <code>@action</code>, add or delete a page, wrap a page in a layout, change the head. The route table is rebuilt and hot-swapped on every change (these previously needed <code>rm -rf .pyxle-build</code> + a restart). Editing <code>pyxle.config.json</code> prints a clear &quot;restart to apply&quot; warning instead of being silently ignored.</li>\n<li><strong><code>pyxle check</code> works on a clean install.</strong> The JSX checker&#39;s parser dependencies are bundled into a self-contained extractor (shipped via <code>pyxle-langkit</code>), so <code>pyxle check</code> runs after <code>pip install &#39;pyxle-framework[langkit]&#39;</code> with no extra npm setup. A missing-dependency failure now names the exact packages instead of an opaque error.</li>\n<li><strong>Locale-independent SSR.</strong> The Python↔Node SSR transport pins UTF-8, so emoji and other non-BMP characters in components or loader data no longer crash rendering under a non-UTF-8 locale (e.g. <code>LANG=C</code>).</li>\n<li><strong>Smoother first run.</strong> <code>pyxle init</code> generates a gitignored <code>.env.local</code> with a random dev <code>PYXLE_SECRET_KEY</code> (CSRF HMAC on out of the box); the scaffold <code>requirements.txt</code> now declares <code>pyxle-framework</code> itself; and <code>pyxle install</code> warns about PEP 668 externally-managed environments with venv guidance instead of letting pip throw a raw wall.</li>\n<li><strong>Docs:</strong> documented calling an <code>@action</code> endpoint directly for scripts and tests.</li>\n</ul>\n<h2 id=\"041\">0.4.1</h2><ul>\n<li><strong>No more double loader run on first load.</strong> The page you land on is now seeded into the client navigation cache from the server render, so the active nav link&#39;s prefetch (a <code>&lt;Link&gt;</code> pointing at the current route) resolves from cache instead of re-fetching. The loader runs <strong>once</strong>, not twice — halving origin work on cold loads and stopping non-idempotent loaders (view counters, analytics) from firing twice. Back/forward navigation to the page is instant for the same reason.</li>\n<li><strong>Per-route navigation-cache TTL.</strong> A route&#39;s <a href=\"/docs/reference/configuration#edge-caching\"><code>cache</code></a> TTL now also governs how long its prefetched/seeded data stays fresh in the client navigation cache, so client freshness matches how long a CDN would serve the page. Routes without a <code>cache</code> entry use a default of <strong>2 minutes</strong> (raised from 30s), tunable via the new <a href=\"/docs/reference/configuration#navigation\"><code>navigation.defaultPrefetchTtl</code></a> option.</li>\n</ul>\n<h2 id=\"040\">0.4.0</h2><ul>\n<li><strong>Edge caching.</strong> Declare cacheable routes in <a href=\"/docs/reference/configuration#edge-caching\"><code>pyxle.config.json::cache</code></a> and pages are served <code>Cache-Control: public, s-maxage=N</code> (plus <code>stale-while-revalidate</code>) so a CDN or reverse proxy can absorb traffic instead of your origin — Pyxle&#39;s config-driven take on per-route revalidation. The per-user CSRF cookie is automatically omitted from cacheable responses. See <a href=\"/docs/guides/deployment#cdn-and-edge-caching\">Deployment → CDN and edge caching</a>.</li>\n<li><strong>Hardened production errors.</strong> SSR render failures — including the SPA-navigation JSON path — are sanitized in the response (no exception type, message, or path leaks to the client) while the full error is written to the server log for operators.</li>\n<li><strong>Faster static serving.</strong> The static-asset middleware indexes public/client paths up front, skipping a per-request filesystem <code>stat</code> + exception on every dynamic request.</li>\n<li><strong>Layout &amp; template loaders.</strong> A <code>layout.pyxl</code> or <code>template.pyxl</code> can declare its own <code>@server</code> loader; its result lands on the component&#39;s <code>data</code> prop, just like a page — so shared UI (nav bars, the signed-in user, the framework version) loads once per request without repeating the loader in every page. See <a href=\"/docs/core-concepts/layouts#layout-data-loaders\">Layouts → Layout data loaders</a>.</li>\n</ul>\n<h2 id=\"030\">0.3.0</h2><ul>\n<li><strong>First-class plugin system.</strong> Compose apps via <code>pyxle.config.json::plugins</code> (Django-style <code>INSTALLED_APPS</code>) — see the <a href=\"/docs/guides/plugins\">Plugins guide</a> and <a href=\"/docs/reference/plugins-api\">Plugins API reference</a>.</li>\n<li><strong>Django-style service access.</strong> Resolve any plugin-registered service with <code>from pyxle.plugins import plugin</code> and <code>plugin(&quot;auth.service&quot;)</code>, or use a typed shortcut shipped by the plugin (e.g. <code>from pyxle_auth import get_auth_service</code>).</li>\n<li><strong>First-party plugins.</strong> Two official plugins land: <a href=\"/docs/plugins/pyxle-db\"><code>pyxle-db</code></a> (SQLite-first with migrations) and <a href=\"/docs/plugins/pyxle-auth\"><code>pyxle-auth</code></a> (email+password sessions, argon2id, rate limits).</li>\n<li><strong>WebSocket endpoints</strong> — <code>pages/api/*.py</code> can export <code>async def websocket(ws)</code> for live updates, chat, log streaming. See the <a href=\"/docs/guides/api-routes#websocket-endpoints\">API Routes guide</a>.</li>\n<li><strong>Client navigation cache with TTL + invalidation.</strong> Loader payloads are cached for 30s by default (tunable) so back/forward navigation is instant. Call <a href=\"/docs/reference/client-api#invalidateurl\"><code>invalidate(url)</code></a> from the client or return <a href=\"/docs/reference/runtime-api#invalidate_routesresponse-urls\"><code>invalidate_routes(response, ...)</code></a> from an <code>@action</code> to keep list views fresh after mutations — automatically honoured by <code>useAction</code> and <code>&lt;Form&gt;</code>.</li>\n<li><strong><code>ActionError</code> is auto-imported</strong> for any <code>.pyxl</code> with an <code>@action</code>. No more <code>NameError: name &#39;ActionError&#39; is not defined</code> on first try.</li>\n<li><strong><code>&lt;Head&gt;</code> coerces multi-part <code>&lt;title&gt;</code> children</strong> into a single string, silencing React&#39;s &quot;title element received an array&quot; warning for the common <code>&lt;title&gt;{name} — Brand&lt;/title&gt;</code> pattern.</li>\n<li><strong>SSR worker pins <code>LANG=en-US.UTF-8</code></strong> by default (override with <code>PYXLE_SSR_LOCALE</code>) so <code>toLocaleString()</code> and other Intl calls stop causing hydration mismatches.</li>\n<li><strong>Vite resolver prefers pinned versions.</strong> <code>pyxle build</code> now runs <code>npm install</code> before falling back to <code>npx --yes vite</code>, so builds honour your <code>package.json</code> pin instead of fetching <code>vite@latest</code>.</li>\n</ul>\n","toc":[{"depth":2,"text":"Unreleased","slug":"unreleased"},{"depth":2,"text":"0.7.0 — 2026-07-09","slug":"070-2026-07-09"},{"depth":3,"text":"Documentation","slug":"documentation"},{"depth":2,"text":"0.6.1 — 2026-07-01","slug":"061-2026-07-01"},{"depth":2,"text":"0.6.0 — 2026-07-01","slug":"060-2026-07-01"},{"depth":2,"text":"0.5.0 — 2026-06-18","slug":"050-2026-06-18"},{"depth":2,"text":"0.4.5","slug":"045"},{"depth":2,"text":"0.4.4","slug":"044"},{"depth":2,"text":"0.4.3","slug":"043"},{"depth":2,"text":"0.4.2","slug":"042"},{"depth":2,"text":"0.4.1","slug":"041"},{"depth":2,"text":"0.4.0","slug":"040"},{"depth":2,"text":"0.3.0","slug":"030"}],"path":"changelog","markdown":"# Changelog\n\nRelease notes for Pyxle. While we're in beta (`0.x`), minor versions may include breaking changes — those are called out explicitly here. To upgrade, run `pip install --upgrade pyxle-framework`.\n\n## Unreleased\n\n- **The [comparison guide](https://pyxle.dev/docs/guides/comparison.md) now names what Pyxle doesn't have yet.** A new \"What Pyxle doesn't have yet\" section honestly lists the current edges — authoring the client in TypeScript (end-to-end Python types work today; TSX authoring is the [next milestone](https://pyxle.dev/docs/guides/fully-typed-pyxle.md)), a first-class test client, automatic image byte- and font-optimization, and i18n — with how to bridge each one now, so the gaps read as roadmap rather than surprises.\n\n- **A fresh project now ships a human `README.md`.** `pyxle init` scaffolded an `AGENTS.md` for AI agents but no README for people; new projects get a README covering prerequisites, the run commands, and the project layout. The `--template` help text no longer says \"(placeholder)\" — it accurately notes only `default` is supported today.\n\n- **New [Debugging guide](https://pyxle.dev/docs/guides/debugging.md), and a more accurate Deployment guide.** The Debugging guide covers both runtimes — reading the error overlay, `breakpoint()`/`pdb` in a loader, server logs in the browser console, the SSR (Node) side (`window is not defined`, `console.log` running twice, the Node inspector), and reading the compiled `.pyxle-build/` output. Deployment fixes: the sample Dockerfile installs Node.js **22.x** (the old `setup_20.x` can ship a below-floor 20.16–20.18 that Vite 7 rejects); the metrics endpoint is documented at its real path `/api/__pyxle/metrics` (opt-in via `metricsEndpoint: true`), not `/metrics`; a new **Database migrations** section covers `pyxle-db`'s startup/`pyxle-db migrate` flow and backward-compatible migrations for zero-downtime deploys; and a **blue-green gotcha** callout explains that the two instances must share an explicit `csrf.cookieName` and the same `PYXLE_SECRET_KEY` or mid-cutover POSTs `403`.\n\n- **`pyxle serve` refuses to start in production without `PYXLE_SECRET_KEY`.** Serving without a signing secret meant CSRF tokens fell back to unsigned double-submit and every signed cookie (sessions, unsubscribe links) was forgeable — previously only a single easily-missed startup warning. `pyxle serve` now fails fast with an actionable message and a one-liner to generate a key. `pyxle dev` still allows a missing key (it uses a public dev fallback), and the check is skipped if you've deliberately set `csrf.enabled=false`. See [Security](https://pyxle.dev/docs/guides/security.md) and [Deployment](https://pyxle.dev/docs/guides/deployment.md).\n- **Security docs now reflect what Pyxle actually sends.** The [Security guide](https://pyxle.dev/docs/guides/security.md#security-headers) previously told you to add `X-Content-Type-Options`, `X-Frame-Options`, and `Referrer-Policy` at your proxy — but Pyxle already sets all three on every response (and uses `X-Frame-Options: SAMEORIGIN`, not the `DENY` the old docs showed). The guide now documents the auto-set baseline and covers only the two headers genuinely left to you: `Content-Security-Policy` (app-specific) and `Strict-Transport-Security` (belongs at your TLS layer). A [vulnerability-disclosure policy](https://github.com/pyxle-dev/pyxle/blob/main/SECURITY.md) (`SECURITY.md`) is now published for every Pyxle repository.\n\n- **A real testing story: `pyxle.testing` helpers and a Testing guide.** \"How do I test my app?\" now has a runnable answer. A `.pyxl` file isn't an importable Python module, so the old advice (`from pages.index import load_home`) raised `ModuleNotFoundError`. New `pyxle.testing.load_loader(\"pages/index.pyxl\")` compiles the page and returns its `@server` loader as a plain `async` function you can call with a fake request; `load_page(...)` returns the whole module so you can reach any `@action` by name. A new [Testing guide](https://pyxle.dev/docs/guides/testing.md) covers unit-testing loaders/actions, the keep-logic-in-plain-modules pattern, and end-to-end tests over HTTP (with the CSRF handshake), and the [runtime internals doc](https://pyxle.dev/docs/architecture/runtime.md) no longer shows the broken import. A first-class in-process `PyxleTestClient` is called out as the next step on the roadmap.\n\n- **`pyxle dev`/`build` now check your Node.js version up front instead of failing later as an opaque Vite crash.** If Node.js is missing or older than the required `20.19` (the Vite 7 floor), the command stops immediately with a clear, actionable message — `Node.js 20.19+ is required, but 20.16.0 is installed.` — rather than printing \"ready\" beside a red Vite fatal and crash-looping. `pyxle init` warns about a below-floor toolchain after scaffolding (non-fatal, since scaffolding needs no Node), and `pyxle serve` warns when serving a prebuilt app on an old Node. The dev server also no longer prints its \"ready\" banner if Vite exits during startup. [Installation](https://pyxle.dev/docs/getting-started/installation.md#troubleshooting) gains a Troubleshooting section mapping the two most common toolchain errors — pip's \"No matching distribution found\" (Python below 3.10) and the Node.js version error — to the fix, and [Quick Start](https://pyxle.dev/docs/getting-started/quick-start.md) restates both version floors up front.\n\n## 0.7.0 — 2026-07-09\n\n- **Fix: `error.pyxl` renders even when an ancestor layout has a `@server` loader.** The error boundary is compiled wrapped in its layout chain like any page, but its render received no layout loader data — the layout component crashed on the missing props and the request silently fell back to the built-in error document (in dev *and* production, with no log). Boundary renders now execute ancestor layout loaders exactly like a normal page; a loader that fails during the boundary render no longer masks the boundary (it renders with error-only props and the failure is logged), and a boundary that itself fails now logs the fallback instead of swapping silently. Also: honest scope wording for `pyxle typecheck` (page JSX isn't checked without `checkJs` — planned, see [the typed-Pyxle roadmap](https://pyxle.dev/docs/guides/fully-typed-pyxle.md)), the pyxle-auth doc header says 0.4.0, the `useAuth` seed claim matches real SSR behavior, the `username-available` `reason` field is documented, six missing guides join the docs index, and three dead anchors are fixed.\n\n- **Pre-release verification fixes.** `PYXLE_SSR_WORKER_CONCURRENCY` now actually reaches the SSR worker (the sanitized spawn environment stripped it, making the documented knob a no-op). `SIGTERM` to `pyxle dev` — what process managers and IDE stop buttons send — tears down the whole child tree (Vite, esbuild, SSR workers) exactly like Ctrl-C instead of orphaning Vite on its port. A failed rebuild now broadcasts to the browser overlay (as the architecture docs promise), and reverting a file to its last-good content after a failed build correctly triggers the recovery rebuild instead of being hash-skipped. Assorted doc corrections: the overlay WebSocket path (`/__pyxle__/overlay`), the scaffold's generated `.env.local` in the project-structure tree, a verbatim `pyxle routes` sample, a legacy-Tailwind-v3 note in the styling guide, and MiB units in the security guide.\n\n- **Fix: React UI libraries that resolve to a CommonJS build no longer crash SSR.** A dependency without an `exports` map (e.g. `lucide-react`, common across the shadcn/ui ecosystem) resolved to its CommonJS build during the server render, whose `require('react')` failed with `Dynamic require of \"react\" is not supported` — a 500 on any page importing it. The SSR bundler now resolves dependencies **ESM-first** (prefers the `module`/ESM entry over CommonJS `main`, as Vite's SSR does), so those packages link cleanly against the runtime's React. A genuinely CommonJS-only package that still `require`s a runtime module now fails with an actionable error naming the module and the page, pointing at an ES-module version or `<ClientOnly>`, instead of the bare esbuild message. See [Third-party packages → CommonJS packages and SSR](https://pyxle.dev/docs/guides/third-party-packages.md#commonjs-packages-and-ssr).\n\n- **Docs: the road to a fully-typed Pyxle.** A new [roadmap page](https://pyxle.dev/docs/guides/fully-typed-pyxle.md) lays out where Pyxle's type story is heading — authoring your client in TypeScript and forwarding Python types across the boundary so your component's `data` is typed from the Python that produced it — grounded in what already works today (`pyxle check`, `pyxle typecheck`, Pydantic `@action` validation). JavaScript remains fully first-class; nothing here changes today's behavior.\n- **Breaking prerequisite: Node.js 20.19+ is now required.** Node 18 is end-of-life; Vite 7 (below) needs Node `>=20.19`. Update your Node before upgrading. The scaffold now declares `\"engines\": { \"node\": \">=20.19\" }`, and [Installation](https://pyxle.dev/docs/getting-started/installation.md) states the new floor.\n- **Modernized scaffold — React 19, Vite 7, and an interactive `pyxle init`.** A fresh project now ships **React 19** and **Vite 7** (with the Vite-7-compatible `@vitejs/plugin-react` 5), and `pyxle init` is **interactive**: polished arrow-key prompts (highlighted defaults, Enter to accept) ask whether you want Tailwind CSS, whether you want shadcn/ui (only when Tailwind is on), and whether to customize the default `@/*` import alias — the value input appears only if you opt in — then scaffold only what you chose. Non-interactive callers (CI, scripts, non-TTY stdin) never block — pass `--yes` for defaults, or the explicit `--tailwind/--no-tailwind`, `--shadcn/--no-shadcn`, and `--import-alias` flags. `pyxle init .` (or `pyxle init` in an empty directory) scaffolds into the current directory and derives the project name from it. Fresh installs pass `npm audit` clean. See [Quick Start](https://pyxle.dev/docs/getting-started/quick-start.md).\n- **Tailwind CSS is now opt-in — and, when chosen, Tailwind v4 wired straight into Vite.** Previously every project got Tailwind v3 through a side-channel (`tailwind.config.cjs`, `postcss.config.cjs`, a pre-compiled `public/styles/tailwind.css`, and a standalone `tailwindcss --watch` process). That whole mechanism is gone. Choose Tailwind at `pyxle init` and you get **Tailwind v4** via the `@tailwindcss/vite` plugin and a single `@import \"tailwindcss\";` CSS entry — no config files, no separate watcher, no output-CSS-in-`public/`, and therefore no \"Tailwind writes into a watched dir\" rebuild loop. Pyxle adds the plugin to the Vite config it generates only when the project depends on `@tailwindcss/vite`. Decline Tailwind and you get a clean baseline where **plain CSS and CSS Modules** work out of the box (both verified in dev and build). CSS Module class names are generated deterministically, so the server-rendered markup and the client bundle match exactly — no React 19 hydration mismatch. See [Styling](https://pyxle.dev/docs/guides/styling.md).\n- **shadcn/ui support.** Choose shadcn at `pyxle init` (it implies Tailwind) and the scaffold pre-configures `components.json`, the `@` import alias, `lib/utils.js`, and a Tailwind v4 stylesheet with the shadcn theme — so `npx shadcn@latest add button` works with no `shadcn init` step. Every project also gets a root `vite.config.js` (re-exporting Pyxle's generated config) and a `jsconfig.json`, so the Vite ecosystem and editors find a config and resolve the import alias. Pyxle resolves that alias in both the Vite client build **and** the SSR runtime, so aliased imports render on the server too. See [Third-party packages → shadcn/ui](https://pyxle.dev/docs/guides/third-party-packages.md#shadcnui).\n- **Fix: streaming SSR no longer serializes across requests.** Concurrent requests to a page that streams (`<Suspense>` / `loading.pyxl`) used to render strictly one at a time: each connection's first byte arrived only after the *previous* stream had fully finished, so several visitors hitting the same slow page saw a time-to-first-byte staircase (roughly 0.5s, 1s, 3s, 6s…) even though each render was mostly idle waiting on its own boundaries. An SSR worker now handles many renders **concurrently** — overlapping requests all begin rendering immediately and interleave, so every visitor gets the shell in tens of milliseconds regardless of how many others are mid-render. Each render's per-request state (pathname, CSRF token, `<Head>` tags, inline styles) is isolated with `AsyncLocalStorage`, so interleaving can never render one visitor's token or head tags into another's page. As part of this, `pyxle serve` now **auto-sizes** its SSR worker pool by default (previously a single process), so production renders in parallel out of the box; `pyxle dev` still runs a single worker. In-worker render concurrency defaults to 16 and is tunable with `PYXLE_SSR_WORKER_CONCURRENCY`. See the [Streaming guide](https://pyxle.dev/docs/guides/streaming.md#concurrent-streams-dont-queue), [Deployment → SSR workers](https://pyxle.dev/docs/guides/deployment.md#ssr-workers), and [Environment variables](https://pyxle.dev/docs/guides/environment-variables.md#ssr-rendering).\n- **`pyxle init` no longer scaffolds a stale framework pin.** The generated `requirements.txt` used to hard-code `pyxle-framework>=0.4.1,<0.5`, so `pyxle install` in a fresh project silently *downgraded* the framework that scaffolded it. The requirement is now rendered from the running version — running `0.7.0` emits `pyxle-framework>=0.7.0,<0.8` (current release up to the next minor).\n- **`pyxle typecheck` now fails fast with a clear error when TypeScript isn't installed.** It previously fell back to `npx --yes tsc`, which resolves npm's placeholder `tsc` package — relaying its banner and ending with a self-contradictory \"Type check failed with 0 error(s)\". The command now checks for a real `tsc` (local `node_modules/.bin`, then global) *before* building and exits with `TypeScript is required for 'pyxle typecheck' — run: npm install --save-dev typescript` (or \"run: npm install\" when it's declared but not installed). A failed `tsc` run that produces no diagnostics now reports the exit code instead of \"0 error(s)\". See [TypeScript guide](https://pyxle.dev/docs/guides/typescript.md).\n- **Scaffolded `AGENTS.md` corrected on auto-injected names.** It claimed `LoaderError` had to be imported from `pyxle.runtime` — false since 0.6.1, and contradicted by the rest of the file. It now consistently documents that `server`, `action`, `LoaderError`, `ActionError`, `ValidationActionError`, and `invalidate_routes` are all auto-injected by the compiler.\n- **`pyxle check` works out of the box — `pyxle-langkit` is now a default dependency.** On a fresh install, `pyxle check` — quick-start step 6 — exited 1 on a brand-new scaffold with \"JSX checker unavailable\" because the JSX layer lived behind the undocumented `[langkit]` extra. The extra is no longer needed: `pip install pyxle-framework` now pulls in `pyxle-langkit`, so the full check (Python syntax, semantics, and JSX) passes on generated files with zero extra steps. The `[langkit]` extra remains for backward compatibility but is now redundant. See [Installation](https://pyxle.dev/docs/getting-started/installation.md) and [Quick Start](https://pyxle.dev/docs/getting-started/quick-start.md).\n- **Fix: the CSRF cookie no longer collides between Pyxle apps on the same host.** The cookie was literally `pyxle-csrf` with `Path=/`, and browsers scope cookies to a host — ignoring the port — so two Pyxle dev servers on `127.0.0.1` kept overwriting each other's token and every action in the other app failed with `403 CSRF token mismatch`. The default cookie name is now namespaced by the app's bind port (`pyxle-csrf-8000`), a stable per-app discriminator in dev *and* behind a production reverse proxy, and the document shell injects the effective name into every page (`window.__PYXLE_CSRF_COOKIE__`) so `<Form>`/`useAction` always read the right cookie. Pin an explicit name with the existing `csrf.cookieName` key. **Upgrade note:** a leftover `pyxle-csrf` cookie from an earlier version is simply ignored — the token re-issues under the namespaced name on the next page load; no action needed. See [Security → CSRF protection](https://pyxle.dev/docs/guides/security.md#csrf-protection) and [Configuration → CSRF](https://pyxle.dev/docs/reference/configuration.md#csrf).\n- **Fix: multipart forms (file uploads) now pass CSRF with a `_csrf_token` field.** Posting an action as `multipart/form-data` with a correct `_csrf_token` field was rejected with `403 CSRF token missing` — the middleware only read urlencoded bodies and the `X-CSRF-Token` header, so a progressive-enhancement `<Form>` containing a file input was broken before hydration. The middleware now stream-parses a multipart body just far enough to obtain `_csrf_token` (which `<Form>` renders before all other fields) and then replays the consumed bytes downstream untouched — file parts are never buffered by the CSRF layer, so uploads of any size work. The pre-token scan is capped at 1 MiB; past the cap (or when the field is absent) the request is rejected with a `403` that says exactly what to do: send the token in the `X-CSRF-Token` header, or place `_csrf_token` before the file fields. `missing` vs `mismatch` errors are now accurate on every path, including a new `CSRF cookie missing` message when a token is submitted without its cookie half. See [Security → Form bodies and uploads](https://pyxle.dev/docs/guides/security.md#form-bodies-and-uploads).\n- **`pyxle dev` output is clean and readable by default, with `--verbose` for the firehose.** The dev console used to dump Vite's raw line-by-line stdout and a wall of internal build chatter, burying the one thing you need — the URL. `pyxle dev` now prints a curated startup summary (the local URL, the Vite URL, the route count, and the total \"ready in X ms\"), a concise one-line notice per incremental rebuild (`Rebuilt … in X ms`), and any warnings or errors — nothing else. The raw `[vite] …` firehose and the step-by-step internals are hidden behind `--verbose` (or the global `pyxle -v dev`), which restores the full output for troubleshooting. Genuine signal — errors, warnings, the URLs, and rebuild success/failure — is always shown. `pyxle serve` (production) output is unchanged. See [CLI → pyxle dev](https://pyxle.dev/docs/reference/cli.md#pyxle-dev).\n- **Server-side logs now stream to the browser console during `pyxle dev`.** Your server-side `logging` output (from loaders, actions, and your own modules) is forwarded over the dev overlay's WebSocket to the browser devtools console, prefixed `[pyxle:server]` and mapped to the matching `console` method (`info`→`console.info`, `warning`→`console.warn`, `error`→`console.error`) — so you can follow server logs without leaving the browser. By default only `INFO`+ from your own loggers is forwarded; `--verbose` also forwards `DEBUG` records and the framework's internal loggers. The bridge is bounded and non-blocking (it never stalls the event loop, drops records when no tab is connected or a send fails, guards against re-entrancy, and throttles bursts), and is strictly development-only — it never runs under `pyxle serve` and never ships in the production bundle. See [CLI → pyxle dev](https://pyxle.dev/docs/reference/cli.md#pyxle-dev) and [Architecture → the error overlay](https://pyxle.dev/docs/architecture/dev-server.md#the-error-overlay).\n- **`public/` changes are picked up on refresh instead of triggering a rebuild (matching Next.js).** Editing, adding, or deleting a file under `public/` no longer runs a build pass or forces a browser reload. Static assets are served live from disk, so an edited file shows up on the next request/refresh, and dev now serves public assets with a revalidating `no-cache` header (previously a one-hour cache masked edits until it expired). The dev server keeps a lightweight watch on `public/` for the sole purpose of keeping its static-file index current, so a **newly added or removed** asset becomes (un)discoverable without restarting `pyxle dev`. This also removes the root cause of the Tailwind reload loop: with `tailwind.config.*` and no PostCSS config, the standalone `tailwindcss --watch` compiler writes `public/styles/tailwind.css` into the watched tree — that write used to trigger a rebuild that let Tailwind re-emit the CSS, a self-sustaining loop (about once a second, most visible on Linux). Because `public/` writes no longer rebuild, the loop cannot occur; genuine CSS edits still hot-reload through the `pages/styles/*.css` source path. The rebuild watch (`pages/` and any `dev.watch` extras) still ignores generated build output — `.pyxle-build`/`__pycache__` trees, `*.pyc` bytecode, and `*.db`/`*.db-wal`/`*.db-shm` journals. See [Styling → Legacy Tailwind v3 projects](https://pyxle.dev/docs/guides/styling.md#legacy-tailwind-v3-projects) and [Architecture → the watcher](https://pyxle.dev/docs/architecture/dev-server.md#the-watcher).\n- **Watch extra directories for hot reload with `dev.watch`, and ignore extra paths with `dev.ignore`.** Pyxle watches `pages/` for hot reload, but a shared Python module imported from outside it (e.g. `lib/` or `components/`) would not trigger a rebuild. A new `dev` config block closes that gap: `dev.watch` lists extra project-relative directories to also watch (a change under one runs the normal rebuild + module reload), and `dev.ignore` lists extra glob patterns to skip. `dev.ignore` is **additive** to the built-in generated-output ignores — those are load-bearing and always stay in force, so a user list can only add to them, never clear them. `dev.watch` entries must resolve inside the project root; a traversal path (`../…`) or absolute path is rejected at config load. Both default empty, and both are dev-only (ignored by `pyxle serve`). See [Configuration → Development](https://pyxle.dev/docs/reference/configuration.md#development).\n\n### Documentation\n- **The flagship WebSockets example no longer crashes SSR when copy-pasted.** The first example in the [WebSockets guide](https://pyxle.dev/docs/guides/websockets.md) passed `window.location.pathname` to `useWebSocket()` — evaluated during the server render, where `window` doesn't exist, so following the guide verbatim 500'd the page. The example now derives the socket path from the route's loader data (the loader returns the `room` segment; the component templates it into the same path the page was served from), and a callout explains the rule: the hook never *connects* during SSR, but its arguments are evaluated there — build paths from props/params, not browser globals.\n- **Fixed a broken `@action` example in the [pyxle-db docs](https://pyxle.dev/docs/plugins/pyxle-db.md).** The request-scoped-access example inserted an undefined `title` instead of reading the request body; it now uses `body = await request.json()` (`request.json` is an async method — subscripting it without awaiting fails at runtime), matching the [Server Actions](https://pyxle.dev/docs/core-concepts/server-actions.md) page.\n- **Documented CSRF on the pyxle-auth credential endpoints.** `POST /auth/login` / `/auth/signup` / `/auth/logout` are CSRF-protected like every state-changing POST, so a raw curl per the old docs got `403` with no explanation. A new [CSRF subsection](https://pyxle.dev/docs/plugins/pyxle-auth.md#csrf) states this, explains the double-submit dance (any GET issues the token cookie; echo it via the `X-CSRF-Token` header), notes that `useAuth()` does it automatically, and shows a working curl sequence.\n- **Documented where pyxle-auth accounts live.** A new [Where accounts live](https://pyxle.dev/docs/plugins/pyxle-auth.md#where-accounts-live) section covers the SQLite database auto-created at `data/app.db` on first run (no manual migration step) and how to point at a production database via pyxle-db's `url` setting; the [pyxle-db settings docs](https://pyxle.dev/docs/plugins/pyxle-db.md#plugin-settings) now also state that the file and its directory are created automatically.\n- **Honest scope note on `pyxle check`.** The [CLI reference](https://pyxle.dev/docs/reference/cli.md#pyxle-check) now spells out that a green check validates syntax and semantics but does not prove a page renders — runtime-only mistakes (e.g. a component reading a loader key that doesn't exist) surface when the page loads, not in `check`. It also notes that as of 0.7.0 the JSX level works out of the box, since pyxle-langkit ships with the framework (previously the `[langkit]` extra).\n- **Prerequisites up front.** The [Introduction](https://pyxle.dev/docs/getting-started/introduction.md) now says on page one that Pyxle needs Python 3.10+ (3.12 recommended) and Node.js 20.19+ with npm, matching [Installation](https://pyxle.dev/docs/getting-started/installation.md).\n- **`Accept: text/markdown` negotiation now follows RFC 9110.** The check used to be a substring match on the raw `Accept` header, which ignored q-values entirely — `text/html, text/markdown;q=0` served Markdown even though `q=0` means *not acceptable*, and `text/html;q=0.9, text/markdown;q=0.8` served Markdown even though HTML was preferred. The header is now properly parsed: media types match by exact type/subtype token (no substrings — `text/markdownish` no longer counts), q-values are honoured (`q=0` excludes; default `1.0`, clamped to `0..1`), wildcards like `*/*` never select Markdown, ties go to Markdown, and malformed headers never raise — they fall back to HTML. Since negotiated responses carry `Vary: Accept`, shared caches now key on the correct variant. New public helper: `pyxle.devserver.llms.markdown_is_acceptable(accept)`. See [AI accessibility → Negotiation rules](https://pyxle.dev/docs/guides/llms.md#negotiation-rules).\n- **The generated `/llms.txt` no longer advertises Markdown that isn't there.** The default index used to emit relative links and to link every concrete page's `.md` URL — including pages whose `.md` URL just `307`-redirects to HTML. It now emits **absolute URLs** derived from the request's scheme and host (proxy headers respected), links `.md` only for pages whose Markdown actually resolves (a co-located `.md`, a `to_markdown` handler, an ancestor `llms.py` — or every page when `autoConvert` is on), and lists the rest at their canonical HTML URL. Custom `llms_txt` hooks are unaffected; `ctx.render_default()` returns the improved index. See [AI accessibility → The /llms.txt index](https://pyxle.dev/docs/guides/llms.md#the-llmstxt-index).\n- **Converted Markdown keeps agents on the Markdown channel** (breaking behavior change). `autoConvert` output — and the public `html_to_markdown()` helper — now rewrites internal page links to their `.md` renditions: `[About](/about)` becomes `[About](/about.md)`, preserving query strings and fragments (`/about?x=1#y` → `/about.md?x=1#y`). External URLs, `mailto:`/`tel:` links, `/api/` routes, asset paths with a file extension, and links already ending in `.md` are untouched, and `wrap_markdown` hooks receive the rewritten Markdown. **Breaking:** `html_to_markdown()` rewrites by default — pass `rewrite_links=False` for the old verbatim behavior. See [AI accessibility → autoConvert](https://pyxle.dev/docs/guides/llms.md#autoconvert-the-lossy-fallback).\n- **Fix: a burst of rapid file saves can no longer kill the dev server.** Two dev-server robustness fixes land together. First, build passes are now serialized and `meta.json` is written atomically and only when its content changed — previously a debounce-timer race could run two rebuilds at once, and one pass reading the build metadata mid-write misdiagnosed it as a schema mismatch and wiped the whole `.pyxle-build` cache, deleting and recreating `vite.config.js`. Vite answers a config change with a full restart, and a restart racing the next rebuild could die (`[vite] process exited`), leaving nothing listening until a manual restart. Second, the Vite subprocess is now properly supervised: **any** exit the dev server didn't initiate — including a \"clean\" exit code 0 — logs a clear warning and relaunches Vite with a short exponential backoff (0.5s, doubling) and a bounded budget (3 consecutive attempts). If relaunching keeps failing, a structured `ViteSupervisionError` is surfaced loudly in the terminal instead of a silently dead asset server.\n- **Fix: touching `request.state.<name>` without the providing plugin now explains itself.** A loader or `@action` that reads a `request.state` attribute nothing populated (most commonly `request.state.db` without the pyxle-db plugin) used to 500 with a bare `AttributeError: 'State' object has no attribute 'db'` and a raw traceback that pointed nowhere. The framework now detects exactly this case and reports it as a structured error with guidance: state attributes are provided by plugins or middleware — e.g. `request.state.db` requires the pyxle-db plugin in `pyxle.config.json` (`\"plugins\": [\"pyxle-db\"]`). In dev the message appears in the error overlay, the action's JSON error, and the server log (with the original traceback chained); in production the response stays generic, as always. Any other `AttributeError` from your code flows through unchanged.\n- **Actionable SSR error when a component touches a browser global.** A component that evaluates `window`, `document`, `navigator`, `localStorage`, `sessionStorage`, `location`, `matchMedia`, or `requestAnimationFrame` at render scope used to fail SSR with a bare `window is not defined` and a traceback of framework frames — naming the route but never your file, with no hint at the fix. Dev mode (error page, overlay, log) now explains that browser globals don't exist during server-side rendering, names the page's `.pyxl` source file, and gives the remedy: move the browser-only code into a `useEffect` hook or an event handler, or render the subtree with `<ClientOnly>` — see the [Client Components guide](https://pyxle.dev/docs/guides/client-components.md). Production responses stay generic (no file paths or internals in the body); the enriched explanation goes to the server log. `ReferenceError`s on other names, and all other render errors, flow through exactly as before.\n\n## 0.6.1 — 2026-07-01\n\nA sharper `pyxle check` — the edit → check → fix loop now catches classes of mistake it used to wave through.\n\n- **`pyxle check` gained a semantic layer.** Beyond Python syntax, it now runs pyflakes over the Python section and reports **undefined names** — e.g. a handler that `raise`s a symbol it never imported — plus unused imports and redefinitions. Compiler-injected runtime names are recognized, so the idiomatic decorators and error classes never read as undefined.\n- **Duplicate `export default` is now caught.** `@babel/parser` accepts two default exports even though the build (esbuild) rejects them — so a build-breaking mistake used to pass `check` and only fail later, pointing into a generated path. It's now flagged at check time, at the real source line.\n- **JSX error lines are now accurate.** A JSX syntax error was always reported at the first line of the JSX section; it now points at the actual line. Babel's misleading \"Unterminated regular expression\" (its lexer's guess for an unclosed `{ }`) also carries a plain-language hint about the real cause.\n- **`LoaderError` and `invalidate_routes` are now auto-injected**, matching `ActionError` / `ValidationActionError`. `raise LoaderError(...)` in a loader just works with no import — closing an inconsistency where the action-side equivalent already did. A name you import or define yourself still takes precedence. See [Runtime API](https://pyxle.dev/docs/reference/runtime-api.md).\n- **`pyxle install --break-system-packages`.** For externally-managed (PEP 668) Python environments without a virtualenv, `pyxle install` now offers the flag and points to it in the no-venv warning. See [CLI → pyxle install](https://pyxle.dev/docs/reference/cli.md#pyxle-install).\n\n## 0.6.0 — 2026-07-01\n\n- **AI accessibility — serve your app as markdown, plus `llms.txt`.** A new opt-in `llms` config block makes every Pyxle app legible to AI assistants and coding agents. Enable it (`\"llms\": true`) and the framework serves a clean **markdown** rendition of each page at its URL with `.md` appended (`/docs/routing.md`), returns markdown from the same URL to requests that send `Accept: text/markdown` (browsers are unaffected), serves an **`/llms.txt`** index, and advertises it with `Link`/`X-Llms-Txt` discovery headers. A page's markdown is resolved from your project — a co-located `<page>.md` file, a `to_markdown` handler in the page's server module, or a `to_markdown` in the nearest ancestor `llms.py` (which scopes to a whole route subtree; `pages/llms.py` is app-wide) — with an opt-in `autoConvert` HTML→markdown fallback and a redirect-to-the-page fallback when nothing resolves. `/llms.txt` comes from a static `public/llms.txt`, a `llms_txt` hook in `pages/llms.py`, or a generated index, and a `wrap_markdown` hook in `pages/llms.py` can frame every `.md` response with a header/footer (e.g. agent navigation hints). Off by default; adds nothing to the page hot path. See the [AI accessibility guide](https://pyxle.dev/docs/guides/llms.md) and [Configuration → AI accessibility](https://pyxle.dev/docs/reference/configuration.md#ai-accessibility).\n\n## 0.5.0 — 2026-06-18\n\nThe depth release: caching/SSG/ISR, streaming SSR, realtime (WebSockets + a\ncross-worker Redis broker), Pydantic-validated actions, observability,\nbackground work, image optimization, and multi-worker serving — built,\ndocumented, and dogfooded on pyxle.dev. Still `0.x` (beta); the two behavior\nchanges below are called out explicitly.\n\n- **Cross-worker realtime — a Redis pub/sub broker.** WebSocket channels (`pyxle.realtime.channel` / `room.publish`) now span worker processes and machines. The default `InProcessBroker` lives in one process, so under `pyxle serve --workers N` a client on one worker never saw messages published on another. Set `PYXLE_REALTIME_BROKER=redis` (with the `pyxle-framework[redis]` extra and `PYXLE_REALTIME_REDIS_URL`) and every worker relays through Redis: a message published on any worker reaches every subscriber on every worker, delivered once per connection. **No code change** — your handler's `channel(ws, \"room\")` / `room.publish(...)` are identical; only the broker behind `app.state.pyxle_broker` changes. The connection is pinged at startup (a bad URL fails loudly) and the listener reconnects automatically. The in-process broker remains the default and is correct for `pyxle dev` and single-worker serve. See [WebSockets → Cross-worker realtime with Redis](https://pyxle.dev/docs/guides/websockets.md#cross-worker-realtime-with-redis).\n- **Fix: streaming SSR now survives production gzip.** Production serves enable gzip, and Starlette's stock `GZipMiddleware` buffers the whole streamed response (it never flushes the compressor between chunks), so a streamed `<Suspense>` page arrived all at once instead of shell-first — defeating streaming for every app, since browsers always send `Accept-Encoding: gzip`. Pyxle now ships a streaming-aware `StreamingGZipMiddleware` (from `pyxle.middleware`) that flushes the compressor after every chunk, so the shell's compressed bytes reach the browser immediately. Compression and streaming are both preserved under `pyxle serve`, no configuration needed. See [Streaming SSR → Streaming survives gzip](https://pyxle.dev/docs/guides/streaming.md#streaming-survives-gzip-in-production).\n- **Fix: dynamic pages are no longer client-nav-cached with stale data.** The client navigation cache previously held every route's loader payload for the default window (2 minutes), so navigating away from and back to a **dynamic** page (a `@server` loader with no declared cache lifetime — e.g. a live feed) showed stale data until a hard refresh. The navigation-cache TTL now mirrors the server's real cacheability: a dynamic page (rendered `private, no-cache`) is **not** nav-cached (TTL `0`), so back/forward always refetches and a just-made mutation shows immediately. Pages opt into nav-caching via a `cache` entry; static, loader-less pages keep the 2-minute default. See [Caching → Client navigation cache](https://pyxle.dev/docs/guides/caching.md#client-navigation-cache) and [Navigation cache TTL](https://pyxle.dev/docs/reference/client-api.md#navigation-cache-ttl).\n- **Fix: `pyxle build --static` pre-renders pages whose loaders use a plugin.** Static pre-rendering ran page and layout loaders without an active plugin context, so any app that touched a plugin service in a loader — most commonly a layout loader calling `pyxle-db`'s `get_database()` — crashed with `No active plugin context` and pre-rendered **nothing** (0 pages). The static builder now stands up the same plugin context a request sees (loads the configured plugins and runs their startup, opening DB connections) around the pre-render, then tears it down — so plugin-backed loaders run at build time exactly as they do at request time, which is how static generation is meant to work (read your data source at build). See [Caching → Static pre-rendering](https://pyxle.dev/docs/guides/caching.md#static-pre-rendering-pyxle-build-static).\n- **Changed (behavior): stricter validation of nested config blocks.** The `cors`, `csrf`, `observability`, and `routeMiddleware` blocks (and `styling` / the `starlette` & `vite` network blocks) now **reject unknown keys at boot** instead of silently ignoring them. A typo'd or mis-cased nested key — e.g. `csrf.cookieSamesite` instead of `cookieSameSite` — that booted before now fails loudly with a clear `Unknown keys in '<block>' block` error. This is a deliberate fail-loud fix: a silently-dropped key could leave a security-relevant default in place. Fix: correct the key name. See [Configuration](https://pyxle.dev/docs/reference/configuration.md).\n- **Fix: `error.pyxl` no longer leaks internal error details in production.** When a loader or SSR render raised a non-author exception, the nearest `error.pyxl` boundary received the raw exception message (and the exception class name) as props — potentially a Node SSR stack trace or a filesystem path — which it could render to end users. In production the boundary now gets a generic `\"An unexpected error occurred.\"` and a sanitized `ServerError` type; author-raised `LoaderError` / `ActionError` messages still pass through (they're meant to be shown). Dev keeps the real (secret-redacted) message. See [Error Handling](https://pyxle.dev/docs/guides/error-handling.md).\n- **Fix: `import.meta.env.PYXLE_PUBLIC_*` is now substituted during SSR.** The Vite `define` for public env vars was applied only to the client bundle, so at server render the expression was `undefined` — any public env var rendered into the initial HTML caused a hydration mismatch / blank first paint. The SSR esbuild step now gets the same `define` (same keys, same quoting), so server and client agree. See [Environment variables → Client-side variables](https://pyxle.dev/docs/guides/environment-variables.md).\n- **Fix: scaffold `.gitignore` no longer ignores `.env`.** A fresh `pyxle init` git-ignored `.env` even though the env-vars doc tells you to commit `.env` / `.env.development` / `.env.production` and ignore only the `.local` variants. The scaffold now matches the doc.\n- **Fix: `<Image>` emits a lowercase `fetchpriority` attribute.** The `priority` image rendered camelCase `fetchPriority`, which React 18.3.1 rejects with an unknown-prop warning on every such image. It now emits the valid lowercase HTML attribute.\n- **Fix: production gzip no longer prints `I/O operation on closed file`.** `StreamingGZipMiddleware` now closes its `GzipFile` + buffer deterministically on every path (small/already-encoded responses, and streams interrupted mid-flight), so the GC no longer finalizes an open compressor and the noisy `Exception ignored while finalizing file … I/O operation on closed file` stderr messages are gone.\n- **Fix: the scaffolded `jsconfig.json` no longer trips TypeScript's `baseUrl` deprecation.** VS Code (TypeScript 6+) flags `baseUrl` as deprecated — slated for removal in TS 7 — so every fresh project opened with a warning on a file Pyxle generated. The scaffold now declares the import alias with tsconfig-relative `paths` only (`\"@/*\": [\"./*\"]`), which resolves identically without `baseUrl`; editors, Vite, the SSR runtime, and shadcn/ui all read the alias the same way as before.\n- **Fix: `pyxle typecheck` works out of the box on current TypeScript.** The generated `tsconfig.json` used `moduleResolution: \"node10\"` and `baseUrl`, both deprecated in TypeScript 7.0, so `tsc` failed with TS5107/TS5101 on a new-enough TypeScript. It now uses `\"bundler\"` resolution (correct for the Vite/esbuild pipeline) with tsconfig-relative `paths` and no `baseUrl`.\n- **Startup warning when a `BaseHTTPMiddleware` is paired with streaming routes.** A custom middleware subclassing Starlette's `BaseHTTPMiddleware` buffers the whole response, which breaks streaming SSR (a `<Suspense>` page would arrive all at once / raise `No response returned` on a mid-stream disconnect). Pyxle now logs a clear warning at startup naming the offending middleware class and pointing to the pure-ASGI pattern. See [Middleware](https://pyxle.dev/docs/guides/middleware.md).\n- **Clear compile-time error for TypeScript syntax in a client block.** A `.pyxl` client block is plain JSX — type annotations (`const x: number`), `interface`, generics, and `as` casts aren't supported there. Previously such syntax slipped past the compiler (Babel parses it) and failed later in esbuild/Vite with a confusing message pointing into a generated `.pyxle-build` path. The compiler now detects it up front and raises a clear error pointing at your `.pyxl` source line: \"TypeScript syntax (…) isn't supported in a .pyxl client block yet — keep the client half plain JSX.\" Detection is AST-based (any TS-only node), so valid JSX — ternaries, object literals, the `as` prop — is never flagged. See [TypeScript](https://pyxle.dev/docs/guides/typescript.md).\n- **New guides: TypeScript and migrating from Flask/Django.** [TypeScript](https://pyxle.dev/docs/guides/typescript.md) documents Pyxle's consumption-not-authoring model — the generated `tsconfig.json` and `.d.ts` declarations for every `pyxle/client` hook/component, the `pyxle typecheck` gate, and the honest limits (no `.tsx` authoring or per-page loader types yet). [Migrating from Flask or Django](https://pyxle.dev/docs/guides/migration-from-flask-django.md) maps the concepts you know (routes, views, templates, forms, the ORM, middleware, config) to their Pyxle equivalents, with a frank list of what has no direct equivalent (admin, `url_for`, flash messages, a core ORM).\n- **`error.pyxl` is now a client-side error boundary too.** The server already renders the nearest `error.pyxl` when a loader or the initial render fails; now that same boundary keeps working in the browser. If a component throws while re-rendering — after a state update, during a client-side navigation, or on a hydration fault — Pyxle catches it and renders the nearest `error.pyxl` in place, instead of React unmounting the page to a blank screen. The boundary receives the **same** `error` prop (`message`, `statusCode`, `type`) on both sides, so one `error.pyxl` renders identically wherever the fault happened. It is transparent until something throws (so hydration is byte-for-byte unchanged) and resets on the next navigation; the nearest `error.pyxl` is resolved per route (closest-ancestor walk-up) and threaded to the client like the streaming `loading.pyxl` boundary. In `pyxle dev` the error overlay still surfaces the full stack on top; in production the boundary is the user-facing fallback. This catches *render* faults — an error in an event handler or an `await` is handled where it occurs (a `try/catch`, or an `ActionError` from `useAction`). See [Error Handling → Client-side errors](https://pyxle.dev/docs/guides/error-handling.md#client-side-errors).\n- **Built-in rate limiting — `pyxle.middleware.RateLimitMiddleware`.** A dependency-free **token-bucket** limiter, enabled from `pyxle.config.json` with no code: `{\"rateLimit\": {\"requests\": 100, \"window\": 60}}`. Each client (keyed by remote IP, or the first `X-Forwarded-For` hop when `trustForwardedFor` is set behind a trusted proxy) gets a bucket of `requests` tokens that refills at `requests / window` per second — so a brief burst is allowed while the sustained rate is bounded, friendlier than a hard fixed window. Over-limit requests get `429 Too Many Requests` with a `Retry-After` header. `exemptPaths` skips health checks and metrics scrapes (segment-boundary matching, like `csrf.exemptPaths`). The limiter sits just inside request observability, so a throttled request still gets a correlation id and is counted, but is rejected before CSRF, static serving, or the handler do any work; the bucket store is bounded by an LRU cap so a flood of unique clients can't grow memory without bound. **Multi-worker caveat:** the store is in-memory and per-process, so under `pyxle serve --workers N` the effective global cap is `N × requests` — for one shared limit, rate-limit at your reverse proxy. Off by default (`requests: 0`). See [Middleware → Rate limiting](https://pyxle.dev/docs/guides/middleware.md#rate-limiting) and [Security → Rate limiting](https://pyxle.dev/docs/guides/security.md#rate-limiting).\n- **Route policies now apply to `@action` endpoints.** Previously, a route hook registered under `routeMiddleware` wrapped page and API routes but **not** `@action` calls — so an auth or rate-limit policy could be silently bypassed by hitting the action endpoint directly. Actions now run through the same route-hook pipeline; register action policies under the new `routeMiddleware.actions` list. Because an action replies with JSON, an action hook should reject with a JSON `401`/`403` rather than an HTML redirect (which is why actions get their own list instead of inheriting the page's hooks). See [Middleware → Route-level hooks](https://pyxle.dev/docs/guides/middleware.md#route-level-hooks).\n- **`pyxle serve --workers 0` auto-detects the core count.** Multi-core serving (`pyxle serve --workers N`, each worker an independent process sharing one socket) already shipped; now `--workers 0` sizes the worker count to the host's CPU cores, so you don't hard-code it per machine. The [Deployment guide](https://pyxle.dev/docs/guides/deployment.md#multi-core-worker-processes) gains a consolidated **per-worker state** table (which features are per-process — page cache, WebSocket pub/sub, metrics, background tasks — and the shared-backend escape hatch for each), **rolling-deploy / graceful-restart** guidance (blue-green via `/readyz`, systemd, and the gunicorn-preload option), and the rationale for the share-nothing, per-worker-SSR-pool design.\n- **Build optimization: responsive `<Image>`, preload hints, and `--analyze`.** `<Image>` (from `pyxle/client`) is now an optimized image on par with Next.js's for everything that doesn't need a server-side optimizer: pass a `loader` (a CDN like Cloudinary/imgix, or a build plugin) and it emits a responsive `srcset` across a device-size ladder; it also gains `fill` mode, `sizes`, `quality`, `objectFit`, and `priority` → `fetchpriority=\"high\"` for the LCP image — plus the existing blur placeholder, `fallbackSrc`, lazy-loading, and layout-shift prevention. Without a loader it stays a clean `<img>` (no fake srcset that re-downloads the full image — resizing needs a real backend, which stays opt-in). Separately, the SSR shell now injects `<link rel=\"modulepreload\">` for each page's entry and the chunks it imports (from the build manifest's import graph), so they download in parallel with HTML parsing; `pyxle build --analyze` prints a dependency-free JS/CSS bundle-size report (raw + gzip); and the generated Vite config carries an explicit `build` block (faster builds — Vite's slow gzip-size reporting is off since `--analyze` covers it). JS/CSS minification, tree-shaking, and code-splitting were already on via Vite's production defaults. See [Build Optimization](https://pyxle.dev/docs/guides/build-optimization.md).\n- **Background tasks & deferred work.** Two ways to run work off the request's critical path. Inside an `@action`, `request.state.background.add_task(fn, *args)` (a Starlette `BackgroundTasks`) runs after the response is sent — ideal for \"send the welcome email after signup\"; returning `{\"background\": [fn, *args]}` is shorthand for a single such task. For fire-and-forget work from anywhere (loaders, actions, API routes), the new `pyxle.tasks.enqueue(fn, *args)` schedules it on a small **in-process** worker pool — coroutine functions are awaited, plain callables run in a thread, a failing task is logged (never takes a worker down), and the queue is bounded (`TaskQueueFull` under overload). The queue starts/stops with the app. **Multi-worker caveat:** it's per-process (like the in-process WebSocket broker), so for durability or cross-worker/exactly-once semantics, hand off to a real job queue — the guide documents Celery, ARQ, and Dramatiq integration. See [Background Tasks](https://pyxle.dev/docs/guides/background-tasks.md).\n- **OpenTelemetry tracing (opt-in).** Emit OpenTelemetry spans for each request plus child spans for the SSR render, `@server` loaders, and `@action` calls, so a slow page surfaces as a drill-down trace. It's the heaviest integration, so it's a **separate** optional extra (`pip install \"pyxle-framework[observability-otel]\"`) and **fully off** by default — with the extra absent, spans are a zero-cost no-op (one boolean check), and the exporter endpoint comes from the standard `OTEL_EXPORTER_OTLP_ENDPOINT` env var. Sampling defaults to 5%. A base Pyxle install pulls in no OpenTelemetry packages; enabling `observability.otel` without the extra fails loudly at startup. See [Observability → OpenTelemetry tracing](https://pyxle.dev/docs/guides/observability.md#opentelemetry-tracing).\n- **`pyxle dev --dashboard` — a live terminal observability panel.** Opt into a compact panel that refreshes every few seconds with request throughput and error rate, status-class breakdown, request/SSR-render/loader/action latency, and the page-cache hit ratio. It reads the same in-process metrics (so it's free on the request path), is **dependency-free** (no `rich`), and is dev-only — in production you'd scrape the metrics endpoint instead. See [Observability → Dev dashboard](https://pyxle.dev/docs/guides/observability.md#dev-dashboard).\n- **Structured access logging.** Opt into one structured log line per request — `method`, `path`, `status`, `duration_ms`, and the request's correlation id — via `observability.accessLog`, in `\"console\"` (human-readable) or `\"json\"` format. The correlation id is bound into a context variable for the request's duration, so your own log calls pick it up too. Works with **no new dependency** (a stdlib JSON/console formatter); the optional `[observability]` extra adds [structlog](https://www.structlog.org/) for richer rendering. Off by default so it won't disrupt an existing log pipeline. See [Observability → Structured logging](https://pyxle.dev/docs/guides/observability.md#structured-logging).\n- **Observability: request IDs, timing, metrics, and richer health probes.** Every request now gets a correlation id — surfaced as the `X-Request-Id` response header and on `request.state.request_id`, readable from any `@server` loader or `@action` — and its duration is measured, along with SSR-render, loader, action, and page-cache (hit/stale/miss) metrics. The request-id and timing are **on by default** (a `uuid4` and two `perf_counter` reads, no I/O, added at the outermost layer so even a CSRF rejection is tagged); an incoming `X-Request-Id` is **not** trusted unless you opt in (`observability.trustIncomingRequestId`), since echoing a client value into logs is a spoofing vector. Metrics are recorded into a bounded, allocation-cheap in-process registry and exposed — **opt-in** — at a Prometheus endpoint (`observability.metricsEndpoint`, optionally bearer-guarded); the page-cache hit ratio comes from the existing server cache. `/readyz` now runs **dependency checks** (today: the SSR worker pool has a live worker) and returns `503` until the server is genuinely ready, while `/healthz` stays a pure liveness probe. All configured under the new [`observability`](https://pyxle.dev/docs/reference/configuration.md#observability) block (`\"observability\": false` disables request-id/timing); the helper `from pyxle.observability import get_request_id` reads the id anywhere. **Multi-worker note:** metrics are per-worker (each series carries a `worker` label) — aggregate at the scraper. Structured logging, a `pyxle dev` dashboard, and OpenTelemetry land next in Phase 2.7. See [Observability](https://pyxle.dev/docs/guides/observability.md).\n- **Typed request validation for `@action` with Pydantic.** An action can now declare a body parameter annotated with a [Pydantic](https://docs.pydantic.dev/) model — `async def register(request, body: Signup)` — and Pyxle parses, coerces, and validates the request body before the action runs, passing in a fully-typed instance. No more hand-written `await request.json()` + key-by-key checks. On failure the action is **not** called and the client gets a `422` with a top-level `fields` map (field path → messages, dotted for nested models and indexed for list items: `address.zip`, `tags.0`). Both `useAction` and `<Form>` surface it — `result.fields` / the reactive `submit.fields`, and `<Form onError={(message, fields) => …}>` — so you can render messages next to each input. For checks Pydantic can't express (uniqueness, cross-field rules) raise the new `ValidationActionError`, which is auto-imported alongside `ActionError` and produces the same `422` shape. Because the body models are real Pydantic models, `pyxle openapi` generates an [OpenAPI 3.1](https://spec.openapis.org/oas/v3.1.0) document of every action's request body straight from your code (`--out openapi.json`), derived from runtime introspection so it always matches what the dispatcher validates. Pydantic is an **optional dependency** — install the extra with `pip install \"pyxle-framework[pydantic]\"`; actions without a model parameter are unaffected, and an action that declares one without Pydantic installed fails with a clear error rather than silently skipping validation. The generated `useAction`/`Form` runtimes now also ship TypeScript declarations (`.d.ts`). See [Server Actions → Validating request bodies](https://pyxle.dev/docs/core-concepts/server-actions.md#validating-request-bodies-with-pydantic).\n- **Fix: a server module that fails to import is no longer cached as a broken empty module.** If a page's compiled server module raised during its first import (e.g. a bad top-level `import`), the half-initialised module was left in `sys.modules`, so every later request for that page silently got an *empty* module instead of re-raising the real error. Failed imports are now evicted, so the error surfaces consistently on each request.\n- **WebSockets — page handlers, a client hook, and pub/sub.** A page `.pyxl` can now export a module-scope `async def websocket(ws)` and Pyxle serves a WebSocket route at the page's path, alongside its HTTP render — the same file handles both protocols, and dynamic segments (`pages/chat/[room].pyxl`) resolve into `ws.path_params`. Detection is by convention (the function name, not a decorator), matching how `pages/api/**` modules already expose a `websocket` callable. The new `pyxle.realtime` module ships a small pub/sub layer: `async with channel(ws, \"room\") as room: await room.publish(msg)` subscribes a connection to a named channel and broadcasts to everyone in it, with stale-connection pruning. Because Starlette runs HTTP middleware only for `http` scope, a WS handler gets **no** middleware — so `pyxle.realtime` also provides `authenticate_websocket(ws)` (resolve the pyxle-auth session from the handshake cookie) and `origin_allowed(ws, origins)` (the WS equivalent of CSRF). On the client, the generated `useWebSocket(path, options?)` hook gives `{ status, send, lastMessage, error }` with JSON parsing and exponential-backoff reconnect, and never opens a socket during SSR. **Multi-worker caveat (called out honestly):** the default in-process broker lives in one process, so under `pyxle serve --workers N` a message published on one worker isn't seen by clients on another — Pyxle logs a warning when it detects this, and the `Broker` protocol lets a Redis-backed broker drop in. See [WebSockets](https://pyxle.dev/docs/guides/websockets.md).\n- **Streaming SSR for `<Suspense>` pages.** A page that wraps a slow part in a React 18 `<Suspense>` boundary now streams: Pyxle flushes the shell (everything outside the boundary) immediately for a fast time-to-first-byte, then streams each boundary's content in as it resolves. It's opt-in and zero-config — the compiler detects `<Suspense>` and only those pages stream; every other page keeps the buffered render byte-for-byte. The page hydrates exactly as before (the browser hydrates the same component), so no app changes are needed beyond adding the boundary. Streaming applies to dynamic, per-request pages only: a publicly cacheable route (`{\"data\", \"revalidate\"}` envelope, `CACHE` directive, or edge `cache` entry) still renders buffered so it can be stored and `ETag`'d, and a streamed response is always `private, no-cache`. The document head is flushed before the component renders, so a streamed page uses its static `HEAD` (a runtime-rendered `<Head>` lands too late — buffered pages are unaffected). A render error before the first byte falls back to the nearest `error.pyxl` rather than emitting a partial document. Built on the Node worker's new multi-frame `renderToPipeableStream` transport. See [Streaming SSR](https://pyxle.dev/docs/guides/streaming.md).\n- **`loading.pyxl` route-level loading states.** A `loading.pyxl` file declares a loading component that wraps the whole route in `<Suspense fallback={<Loading/>}>` — streamed as the shell on the server and applied identically on the client so hydration matches. The nearest `loading.pyxl` wins (directory walk-up, like `error.pyxl`), and it composes with a page's own inner `<Suspense>`. Honest caveat: because the `@server` loader runs before render, the fallback only appears when the *render* suspends (a child using `React.lazy` or a thrown promise; Pyxle ships React 18.3.1, which has no `React.use`) — a page whose data comes entirely from its loader never suspends and streams at once. See [Streaming SSR → Route-level loading states](https://pyxle.dev/docs/guides/streaming.md#route-level-loading-states-with-loadingpyxl).\n- **Server-side page caching with incremental regeneration.** A `@server` loader can now return a `{\"data\": ..., \"revalidate\": N}` envelope to cache its rendered HTML for `N` seconds — later requests are served from the cache without re-running the loader or the Node SSR render. A loader-less page can opt in the same way with a module-level `CACHE = {\"revalidate\": N}` directive. And `pyxle build --static` pre-renders every loader-less, non-dynamic page to HTML at build time and warms the cache from it on startup, so the first request after a deploy is a hit — no cold render. Once the window passes, the stale page is served instantly while a single background re-render refreshes it (incremental static regeneration — no thundering herd). Purge a route on demand from an action with `await cache.invalidate(\"/path\")` (or `cache.invalidate_all()`). Routes already declared in the `pyxle.config.json` edge `cache` block are server-cached automatically under the same TTL. Cached responses carry a strong `ETag` (so conditional requests get a `304`) and an `x-pyxle-cache: HIT|STALE|MISS` header. The cache is enabled for `pyxle serve` (off in `pyxle dev`) and stores to a bounded in-memory backend by default, or to local disk or a shared Redis via `PYXLE_PAGE_CACHE_BACKEND` (Redis needs `pip install 'pyxle-framework[redis]'`). Only cache pages that render no per-user data — a cached render is shared with every visitor. See [Caching](https://pyxle.dev/docs/guides/caching.md).\n\n## 0.4.5\n\n- **Signed cookies & tokens — `sign_cookie` / `verify_cookie`.** New stdlib-only helpers (importable from `pyxle`) attach a tamper-proof HMAC-SHA256 signature to any string — a session id, a \"remember me\" token, a stateless unsubscribe or password-reset link — and verify it later in constant time. The secret comes from `PYXLE_SECRET_KEY` (or an explicit `secret_key=`), and a `salt=` namespaces signatures so a token minted for one purpose can't be replayed for another. Signing without a secret raises `MissingSecretKeyError` rather than returning an unprotected value — it fails closed. See [Security → Signed cookies and tokens](https://pyxle.dev/docs/guides/security.md#signed-cookies-and-tokens).\n- **Fix: `PYXLE_PUBLIC_*` client env vars now work in `pyxle dev` and no longer break `pyxle build`.** The generated Vite `define` emitted each value as a bare expression, so a non-identifier value (an API URL, a `0x…` Turnstile key) was silently dropped in dev and crashed `vite build` with esbuild's \"Invalid define value\". Values are now emitted as quoted JS string literals, so `import.meta.env.PYXLE_PUBLIC_*` resolves correctly in both dev and production builds. See [Environment variables → Client-side variables](https://pyxle.dev/docs/guides/environment-variables.md).\n- **Fix: web fonts and other CSS `url()` assets no longer 404 in `pyxle dev`.** Vite rewrites `@font-face`/`url(...)` references to root-relative `/@fs/…` paths, which the browser resolved against Pyxle's origin instead of Vite's and 404'd — so `@fontsource` `.woff2` files silently failed to load in development (production builds were unaffected, as their assets are hashed and served by Pyxle). Pyxle now sets Vite's `server.origin`, so dev assets load from Vite directly.\n\n## 0.4.4\n\n- **Fix: cross-page hash links scroll to their anchor.** Client-side navigation to `/page#section` changed the page but stayed pinned to the top (only the URL carried the hash); a full load scrolled correctly. The router now jumps to the top and then scrolls to the anchor as soon as the next page's DOM commits, matching native behaviour — including for unknown anchors, which stay at the top.\n\n## 0.4.3\n\n- **Security: hardened HEAD sanitisation (XSS).** Dynamic `HEAD` values/callables that interpolate loader data into raw head strings — the documented dynamic-meta-tags recipe — could previously inject markup because attribute values weren't escaped (a `\"` in a `<meta content>` broke out of the attribute). Head elements are now parsed and rebuilt from a strict tag allowlist with every attribute value HTML-escaped, `on*` handlers dropped, `javascript:`/`vbscript:`/`data:` URLs neutralised, `<meta http-equiv=\"refresh\">` and non-head tags rejected, and any markup injected after a breakout discarded. Inline `<script>`/`<style>` head content remains supported as trusted author code. Self-closing head tags now serialise canonically as `/>`.\n- **Security: CSRF exempt paths match on segment boundaries.** `csrf.exemptPaths` previously used a bare string prefix, so exempting `/api/webhooks` also exempted an adjacently-named sibling like `/api/webhooks-admin`. Exemptions now match a path exactly or at a `/` boundary.\n- **Security: oversized no-JS form POSTs fail loud.** A form-encoded POST whose body exceeded the CSRF buffer cap (1 MB) was silently truncated and replayed to your handler. Such requests now return `413` and ask for the token via the `X-CSRF-Token` header instead of corrupting the payload.\n- **Fix: `<Script>`/`<Image>` boolean attributes written as strings.** `defer=\"false\"`, `async=\"no\"`, `priority=\"0\"`, `lazy=\"false\"` and similar quoted-falsy forms were coerced to `True` (the opposite of the source). They now coerce correctly.\n- **Fix: custom `csrf.cookieName` / `csrf.headerName` now reach the client runtime.** Configuring either name made every action POST fail with `403` — the middleware set and validated the cookie under the configured name while `useAction`/`<Form>` kept reading a literal `pyxle-csrf` cookie and sending a literal `x-csrf-token` header. Non-default names are now injected into the document shell as `window.__PYXLE_CSRF_COOKIE__` / `window.__PYXLE_CSRF_HEADER__` and the client runtime resolves them with the defaults as fallback, in both dev and production builds.\n- **Fix: no double loader run on hover-then-click navigation.** When a `<Link>` was prefetched on hover/viewport and then clicked, the click raced a second request and ran the page's `@server` loader twice. The click now reuses the in-flight prefetch, and a navigation-sequence token discards a superseded prefetch so a slow one can't render a stale page over a newer click.\n- **Multi-core serving: `pyxle serve --workers N`.** Production serving can now run N independent server processes on one port (each with its own SSR pool and event loop) — throughput scales with cores, no load balancer or shared state to configure. See [Deployment → Multi-core (worker processes)](https://pyxle.dev/docs/guides/deployment.md#multi-core-worker-processes).\n- **Sync API endpoints.** A plain `def endpoint(request)` in `pages/api/` now runs in Starlette's threadpool instead of failing at request time — blocking database drivers and sync SDKs no longer require manual `asyncio.to_thread` wrapping. Sync `get`/`post`/… methods on `HTTPEndpoint` classes are threadpooled too, and `HTTPEndpoint` classes dispatch correctly alongside route hooks. See [API Routes → Sync endpoints and blocking calls](https://pyxle.dev/docs/guides/api-routes.md#sync-endpoints-and-blocking-calls).\n- **In-memory static asset cache.** When serving a production build, small static files (≤1 MB each, 32 MB total per process) are loaded into memory at startup and served without filesystem I/O or threadpool hops — several-fold higher static throughput. Conditional requests (`ETag`/`If-Modified-Since` → 304) and cache headers behave exactly as before; larger files keep streaming from disk.\n\n## 0.4.2\n\n- **Live dev-server reconciliation.** Editing a `.pyxl` in `pyxle dev` now applies route-*shape* changes without a restart — rename/add/remove a `@server` loader or `@action`, add or delete a page, wrap a page in a layout, change the head. The route table is rebuilt and hot-swapped on every change (these previously needed `rm -rf .pyxle-build` + a restart). Editing `pyxle.config.json` prints a clear \"restart to apply\" warning instead of being silently ignored.\n- **`pyxle check` works on a clean install.** The JSX checker's parser dependencies are bundled into a self-contained extractor (shipped via `pyxle-langkit`), so `pyxle check` runs after `pip install 'pyxle-framework[langkit]'` with no extra npm setup. A missing-dependency failure now names the exact packages instead of an opaque error.\n- **Locale-independent SSR.** The Python↔Node SSR transport pins UTF-8, so emoji and other non-BMP characters in components or loader data no longer crash rendering under a non-UTF-8 locale (e.g. `LANG=C`).\n- **Smoother first run.** `pyxle init` generates a gitignored `.env.local` with a random dev `PYXLE_SECRET_KEY` (CSRF HMAC on out of the box); the scaffold `requirements.txt` now declares `pyxle-framework` itself; and `pyxle install` warns about PEP 668 externally-managed environments with venv guidance instead of letting pip throw a raw wall.\n- **Docs:** documented calling an `@action` endpoint directly for scripts and tests.\n\n## 0.4.1\n\n- **No more double loader run on first load.** The page you land on is now seeded into the client navigation cache from the server render, so the active nav link's prefetch (a `<Link>` pointing at the current route) resolves from cache instead of re-fetching. The loader runs **once**, not twice — halving origin work on cold loads and stopping non-idempotent loaders (view counters, analytics) from firing twice. Back/forward navigation to the page is instant for the same reason.\n- **Per-route navigation-cache TTL.** A route's [`cache`](https://pyxle.dev/docs/reference/configuration.md#edge-caching) TTL now also governs how long its prefetched/seeded data stays fresh in the client navigation cache, so client freshness matches how long a CDN would serve the page. Routes without a `cache` entry use a default of **2 minutes** (raised from 30s), tunable via the new [`navigation.defaultPrefetchTtl`](https://pyxle.dev/docs/reference/configuration.md#navigation) option.\n\n## 0.4.0\n\n- **Edge caching.** Declare cacheable routes in [`pyxle.config.json::cache`](https://pyxle.dev/docs/reference/configuration.md#edge-caching) and pages are served `Cache-Control: public, s-maxage=N` (plus `stale-while-revalidate`) so a CDN or reverse proxy can absorb traffic instead of your origin — Pyxle's config-driven take on per-route revalidation. The per-user CSRF cookie is automatically omitted from cacheable responses. See [Deployment → CDN and edge caching](https://pyxle.dev/docs/guides/deployment.md#cdn-and-edge-caching).\n- **Hardened production errors.** SSR render failures — including the SPA-navigation JSON path — are sanitized in the response (no exception type, message, or path leaks to the client) while the full error is written to the server log for operators.\n- **Faster static serving.** The static-asset middleware indexes public/client paths up front, skipping a per-request filesystem `stat` + exception on every dynamic request.\n- **Layout & template loaders.** A `layout.pyxl` or `template.pyxl` can declare its own `@server` loader; its result lands on the component's `data` prop, just like a page — so shared UI (nav bars, the signed-in user, the framework version) loads once per request without repeating the loader in every page. See [Layouts → Layout data loaders](https://pyxle.dev/docs/core-concepts/layouts.md#layout-data-loaders).\n\n## 0.3.0\n\n- **First-class plugin system.** Compose apps via `pyxle.config.json::plugins` (Django-style `INSTALLED_APPS`) — see the [Plugins guide](https://pyxle.dev/docs/guides/plugins.md) and [Plugins API reference](https://pyxle.dev/docs/reference/plugins-api.md).\n- **Django-style service access.** Resolve any plugin-registered service with `from pyxle.plugins import plugin` and `plugin(\"auth.service\")`, or use a typed shortcut shipped by the plugin (e.g. `from pyxle_auth import get_auth_service`).\n- **First-party plugins.** Two official plugins land: [`pyxle-db`](https://pyxle.dev/docs/plugins/pyxle-db.md) (SQLite-first with migrations) and [`pyxle-auth`](https://pyxle.dev/docs/plugins/pyxle-auth.md) (email+password sessions, argon2id, rate limits).\n- **WebSocket endpoints** — `pages/api/*.py` can export `async def websocket(ws)` for live updates, chat, log streaming. See the [API Routes guide](https://pyxle.dev/docs/guides/api-routes.md#websocket-endpoints).\n- **Client navigation cache with TTL + invalidation.** Loader payloads are cached for 30s by default (tunable) so back/forward navigation is instant. Call [`invalidate(url)`](https://pyxle.dev/docs/reference/client-api.md#invalidateurl) from the client or return [`invalidate_routes(response, ...)`](https://pyxle.dev/docs/reference/runtime-api.md#invalidate_routesresponse-urls) from an `@action` to keep list views fresh after mutations — automatically honoured by `useAction` and `<Form>`.\n- **`ActionError` is auto-imported** for any `.pyxl` with an `@action`. No more `NameError: name 'ActionError' is not defined` on first try.\n- **`<Head>` coerces multi-part `<title>` children** into a single string, silencing React's \"title element received an array\" warning for the common `<title>{name} — Brand</title>` pattern.\n- **SSR worker pins `LANG=en-US.UTF-8`** by default (override with `PYXLE_SSR_LOCALE`) so `toLocaleString()` and other Intl calls stop causing hydration mismatches.\n- **Vite resolver prefers pinned versions.** `pyxle build` now runs `npm install` before falling back to `npx --yes vite`, so builds honour your `package.json` pin instead of fetching `vite@latest`.\n","prev":{"title":"Frequently Asked Questions","path":"faq"}}