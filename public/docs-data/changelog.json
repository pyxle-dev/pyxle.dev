{"title":"Changelog","description":"Release notes for Pyxle. While we're in beta (`0.x`), minor versions may include breaking changes — those are called out explicitly here. To upgrade, run `pip install --upgrade pyxle-framework`.","html":"<p>Release notes for Pyxle. While we&#39;re in beta (<code>0.x</code>), minor versions may include breaking changes — those are called out explicitly here. To upgrade, run <code>pip install --upgrade pyxle-framework</code>.</p>\n<h2 id=\"unreleased\">Unreleased</h2><ul>\n<li><strong>OpenTelemetry tracing (opt-in).</strong> Emit OpenTelemetry spans for each request plus child spans for the SSR render, <code>@server</code> loaders, and <code>@action</code> calls, so a slow page surfaces as a drill-down trace. It&#39;s the heaviest integration, so it&#39;s a <strong>separate</strong> optional extra (<code>pip install &quot;pyxle-framework[observability-otel]&quot;</code>) and <strong>fully off</strong> by default — with the extra absent, spans are a zero-cost no-op (one boolean check), and the exporter endpoint comes from the standard <code>OTEL_EXPORTER_OTLP_ENDPOINT</code> env var. Sampling defaults to 5%. A base Pyxle install pulls in no OpenTelemetry packages; enabling <code>observability.otel</code> without the extra fails loudly at startup. See <a href=\"/docs/guides/observability#opentelemetry-tracing\">Observability → OpenTelemetry tracing</a>.</li>\n<li><strong><code>pyxle dev --dashboard</code> — a live terminal observability panel.</strong> Opt into a compact panel that refreshes every few seconds with request throughput and error rate, status-class breakdown, request/SSR-render/loader/action latency, and the page-cache hit ratio. It reads the same in-process metrics (so it&#39;s free on the request path), is <strong>dependency-free</strong> (no <code>rich</code>), and is dev-only — in production you&#39;d scrape the metrics endpoint instead. See <a href=\"/docs/guides/observability#dev-dashboard\">Observability → Dev dashboard</a>.</li>\n<li><strong>Structured access logging.</strong> Opt into one structured log line per request — <code>method</code>, <code>path</code>, <code>status</code>, <code>duration_ms</code>, and the request&#39;s correlation id — via <code>observability.accessLog</code>, in <code>&quot;console&quot;</code> (human-readable) or <code>&quot;json&quot;</code> format. The correlation id is bound into a context variable for the request&#39;s duration, so your own log calls pick it up too. Works with <strong>no new dependency</strong> (a stdlib JSON/console formatter); the optional <code>[observability]</code> extra adds <a href=\"https://www.structlog.org/\" target=\"_blank\" rel=\"noreferrer\">structlog</a> for richer rendering. Off by default so it won&#39;t disrupt an existing log pipeline. See <a href=\"/docs/guides/observability#structured-logging\">Observability → Structured logging</a>.</li>\n<li><strong>Observability: request IDs, timing, metrics, and richer health probes.</strong> Every request now gets a correlation id — surfaced as the <code>X-Request-Id</code> response header and on <code>request.state.request_id</code>, readable from any <code>@server</code> loader or <code>@action</code> — and its duration is measured, along with SSR-render, loader, action, and page-cache (hit/stale/miss) metrics. The request-id and timing are <strong>on by default</strong> (a <code>uuid4</code> and two <code>perf_counter</code> reads, no I/O, added at the outermost layer so even a CSRF rejection is tagged); an incoming <code>X-Request-Id</code> is <strong>not</strong> trusted unless you opt in (<code>observability.trustIncomingRequestId</code>), since echoing a client value into logs is a spoofing vector. Metrics are recorded into a bounded, allocation-cheap in-process registry and exposed — <strong>opt-in</strong> — at a Prometheus endpoint (<code>observability.metricsEndpoint</code>, optionally bearer-guarded); the page-cache hit ratio comes from the existing server cache. <code>/readyz</code> now runs <strong>dependency checks</strong> (today: the SSR worker pool has a live worker) and returns <code>503</code> until the server is genuinely ready, while <code>/healthz</code> stays a pure liveness probe. All configured under the new <a href=\"/docs/reference/configuration#observability\">`observability`</a> block (<code>&quot;observability&quot;: false</code> disables request-id/timing); the helper <code>from pyxle.observability import get_request_id</code> reads the id anywhere. <strong>Multi-worker note:</strong> metrics are per-worker (each series carries a <code>worker</code> label) — aggregate at the scraper. Structured logging, a <code>pyxle dev</code> dashboard, and OpenTelemetry land next in Phase 2.7. See <a href=\"/docs/guides/observability\">Observability</a>.</li>\n<li><strong>Typed request validation for <code>@action</code> with Pydantic.</strong> An action can now declare a body parameter annotated with a <a href=\"https://docs.pydantic.dev/\" target=\"_blank\" rel=\"noreferrer\">Pydantic</a> model — <code>async def register(request, body: Signup)</code> — and Pyxle parses, coerces, and validates the request body before the action runs, passing in a fully-typed instance. No more hand-written <code>await request.json()</code> + key-by-key checks. On failure the action is <strong>not</strong> called and the client gets a <code>422</code> with a top-level <code>fields</code> map (field path → messages, dotted for nested models and indexed for list items: <code>address.zip</code>, <code>tags.0</code>). Both <code>useAction</code> and <code>&lt;Form&gt;</code> surface it — <code>result.fields</code> / the reactive <code>submit.fields</code>, and <code>&lt;Form onError={(message, fields) =&gt; …}&gt;</code> — so you can render messages next to each input. For checks Pydantic can&#39;t express (uniqueness, cross-field rules) raise the new <code>ValidationActionError</code>, which is auto-imported alongside <code>ActionError</code> and produces the same <code>422</code> shape. Because the body models are real Pydantic models, <code>pyxle openapi</code> generates an <a href=\"https://spec.openapis.org/oas/v3.1.0\" target=\"_blank\" rel=\"noreferrer\">OpenAPI 3.1</a> document of every action&#39;s request body straight from your code (<code>--out openapi.json</code>), derived from runtime introspection so it always matches what the dispatcher validates. Pydantic is an <strong>optional dependency</strong> — install the extra with <code>pip install &quot;pyxle-framework[pydantic]&quot;</code>; actions without a model parameter are unaffected, and an action that declares one without Pydantic installed fails with a clear error rather than silently skipping validation. The generated <code>useAction</code>/<code>Form</code> runtimes now also ship TypeScript declarations (<code>.d.ts</code>). See <a href=\"/docs/core-concepts/server-actions#validating-request-bodies-with-pydantic\">Server Actions → Validating request bodies</a>.</li>\n<li><strong>Fix: a server module that fails to import is no longer cached as a broken empty module.</strong> If a page&#39;s compiled server module raised during its first import (e.g. a bad top-level <code>import</code>), the half-initialised module was left in <code>sys.modules</code>, so every later request for that page silently got an <em>empty</em> module instead of re-raising the real error. Failed imports are now evicted, so the error surfaces consistently on each request.</li>\n<li><strong>WebSockets — page handlers, a client hook, and pub/sub.</strong> A page <code>.pyxl</code> can now export a module-scope <code>async def websocket(ws)</code> and Pyxle serves a WebSocket route at the page&#39;s path, alongside its HTTP render — the same file handles both protocols, and dynamic segments (<code>pages/chat/[room].pyxl</code>) resolve into <code>ws.path_params</code>. Detection is by convention (the function name, not a decorator), matching how <code>pages/api/**</code> modules already expose a <code>websocket</code> callable. The new <code>pyxle.realtime</code> module ships a small pub/sub layer: <code>async with channel(ws, &quot;room&quot;) as room: await room.publish(msg)</code> subscribes a connection to a named channel and broadcasts to everyone in it, with stale-connection pruning. Because Starlette runs HTTP middleware only for <code>http</code> scope, a WS handler gets <strong>no</strong> middleware — so <code>pyxle.realtime</code> also provides <code>authenticate_websocket(ws)</code> (resolve the pyxle-auth session from the handshake cookie) and <code>origin_allowed(ws, origins)</code> (the WS equivalent of CSRF). On the client, the generated <code>useWebSocket(path, options?)</code> hook gives <code>{ status, send, lastMessage, error }</code> with JSON parsing and exponential-backoff reconnect, and never opens a socket during SSR. <strong>Multi-worker caveat (called out honestly):</strong> the default in-process broker lives in one process, so under <code>pyxle serve --workers N</code> a message published on one worker isn&#39;t seen by clients on another — Pyxle logs a warning when it detects this, and the <code>Broker</code> protocol lets a Redis-backed broker drop in. See <a href=\"/docs/guides/websockets\">WebSockets</a>.</li>\n<li><strong>Streaming SSR for <code>&lt;Suspense&gt;</code> pages.</strong> A page that wraps a slow part in a React 18 <code>&lt;Suspense&gt;</code> boundary now streams: Pyxle flushes the shell (everything outside the boundary) immediately for a fast time-to-first-byte, then streams each boundary&#39;s content in as it resolves. It&#39;s opt-in and zero-config — the compiler detects <code>&lt;Suspense&gt;</code> and only those pages stream; every other page keeps the buffered render byte-for-byte. The page hydrates exactly as before (the browser hydrates the same component), so no app changes are needed beyond adding the boundary. Streaming applies to dynamic, per-request pages only: a publicly cacheable route (<code>{&quot;data&quot;, &quot;revalidate&quot;}</code> envelope, <code>CACHE</code> directive, or edge <code>cache</code> entry) still renders buffered so it can be stored and <code>ETag</code>&#39;d, and a streamed response is always <code>private, no-cache</code>. The document head is flushed before the component renders, so a streamed page uses its static <code>HEAD</code> (a runtime-rendered <code>&lt;Head&gt;</code> lands too late — buffered pages are unaffected). A render error before the first byte falls back to the nearest <code>error.pyxl</code> rather than emitting a partial document. Built on the Node worker&#39;s new multi-frame <code>renderToPipeableStream</code> transport. See <a href=\"/docs/guides/streaming\">Streaming SSR</a>.</li>\n<li><strong><code>loading.pyxl</code> route-level loading states.</strong> A <code>loading.pyxl</code> file declares a loading component that wraps the whole route in <code>&lt;Suspense fallback={&lt;Loading/&gt;}&gt;</code> — streamed as the shell on the server and applied identically on the client so hydration matches. The nearest <code>loading.pyxl</code> wins (directory walk-up, like <code>error.pyxl</code>), and it composes with a page&#39;s own inner <code>&lt;Suspense&gt;</code>. Honest caveat: because the <code>@server</code> loader runs before render, the fallback only appears when the <em>render</em> suspends (a child using <code>React.lazy</code>, <code>use()</code>, or a thrown promise) — a page whose data comes entirely from its loader never suspends and streams at once. See <a href=\"/docs/guides/streaming#route-level-loading-states-with-loadingpyxl\">Streaming SSR → Route-level loading states</a>.</li>\n<li><strong>Server-side page caching with incremental regeneration.</strong> A <code>@server</code> loader can now return a <code>{&quot;data&quot;: ..., &quot;revalidate&quot;: N}</code> envelope to cache its rendered HTML for <code>N</code> seconds — later requests are served from the cache without re-running the loader or the Node SSR render. A loader-less page can opt in the same way with a module-level <code>CACHE = {&quot;revalidate&quot;: N}</code> directive. And <code>pyxle build --static</code> pre-renders every loader-less, non-dynamic page to HTML at build time and warms the cache from it on startup, so the first request after a deploy is a hit — no cold render. Once the window passes, the stale page is served instantly while a single background re-render refreshes it (incremental static regeneration — no thundering herd). Purge a route on demand from an action with <code>await cache.invalidate(&quot;/path&quot;)</code> (or <code>cache.invalidate_all()</code>). Routes already declared in the <code>pyxle.config.json</code> edge <code>cache</code> block are server-cached automatically under the same TTL. Cached responses carry a strong <code>ETag</code> (so conditional requests get a <code>304</code>) and an <code>x-pyxle-cache: HIT|STALE|MISS</code> header. The cache is enabled for <code>pyxle serve</code> (off in <code>pyxle dev</code>) and stores to a bounded in-memory backend by default, or to local disk or a shared Redis via <code>PYXLE_PAGE_CACHE_BACKEND</code> (Redis needs <code>pip install &#39;pyxle-framework[redis]&#39;</code>). Only cache pages that render no per-user data — a cached render is shared with every visitor. See <a href=\"/docs/guides/caching\">Caching</a>.</li>\n</ul>\n<h2 id=\"045\">0.4.5</h2><ul>\n<li><strong>Signed cookies &amp; tokens — <code>sign_cookie</code> / <code>verify_cookie</code>.</strong> New stdlib-only helpers (importable from <code>pyxle</code>) attach a tamper-proof HMAC-SHA256 signature to any string — a session id, a &quot;remember me&quot; token, a stateless unsubscribe or password-reset link — and verify it later in constant time. The secret comes from <code>PYXLE_SECRET_KEY</code> (or an explicit <code>secret_key=</code>), and a <code>salt=</code> namespaces signatures so a token minted for one purpose can&#39;t be replayed for another. Signing without a secret raises <code>MissingSecretKeyError</code> rather than returning an unprotected value — it fails closed. See <a href=\"/docs/guides/security#signed-cookies-and-tokens\">Security → Signed cookies and tokens</a>.</li>\n<li><strong>Fix: <code>PYXLE_PUBLIC_*</code> client env vars now work in <code>pyxle dev</code> and no longer break <code>pyxle build</code>.</strong> The generated Vite <code>define</code> emitted each value as a bare expression, so a non-identifier value (an API URL, a <code>0x…</code> Turnstile key) was silently dropped in dev and crashed <code>vite build</code> with esbuild&#39;s &quot;Invalid define value&quot;. Values are now emitted as quoted JS string literals, so <code>import.meta.env.PYXLE_PUBLIC_*</code> resolves correctly in both dev and production builds. See <a href=\"/docs/guides/environment-variables\">Environment variables → Client-side variables</a>.</li>\n<li><strong>Fix: web fonts and other CSS <code>url()</code> assets no longer 404 in <code>pyxle dev</code>.</strong> Vite rewrites <code>@font-face</code>/<code>url(...)</code> references to root-relative <code>/@fs/…</code> paths, which the browser resolved against Pyxle&#39;s origin instead of Vite&#39;s and 404&#39;d — so <code>@fontsource</code> <code>.woff2</code> files silently failed to load in development (production builds were unaffected, as their assets are hashed and served by Pyxle). Pyxle now sets Vite&#39;s <code>server.origin</code>, so dev assets load from Vite directly.</li>\n</ul>\n<h2 id=\"044\">0.4.4</h2><ul>\n<li><strong>Fix: cross-page hash links scroll to their anchor.</strong> Client-side navigation to <code>/page#section</code> changed the page but stayed pinned to the top (only the URL carried the hash); a full load scrolled correctly. The router now jumps to the top and then scrolls to the anchor as soon as the next page&#39;s DOM commits, matching native behaviour — including for unknown anchors, which stay at the top.</li>\n</ul>\n<h2 id=\"043\">0.4.3</h2><ul>\n<li><strong>Security: hardened HEAD sanitisation (XSS).</strong> Dynamic <code>HEAD</code> values/callables that interpolate loader data into raw head strings — the documented dynamic-meta-tags recipe — could previously inject markup because attribute values weren&#39;t escaped (a <code>&quot;</code> in a <code>&lt;meta content&gt;</code> broke out of the attribute). Head elements are now parsed and rebuilt from a strict tag allowlist with every attribute value HTML-escaped, <code>on*</code> handlers dropped, <code>javascript:</code>/<code>vbscript:</code>/<code>data:</code> URLs neutralised, <code>&lt;meta http-equiv=&quot;refresh&quot;&gt;</code> and non-head tags rejected, and any markup injected after a breakout discarded. Inline <code>&lt;script&gt;</code>/<code>&lt;style&gt;</code> head content remains supported as trusted author code. Self-closing head tags now serialise canonically as <code>/&gt;</code>.</li>\n<li><strong>Security: CSRF exempt paths match on segment boundaries.</strong> <code>csrf.exemptPaths</code> previously used a bare string prefix, so exempting <code>/api/webhooks</code> also exempted an adjacently-named sibling like <code>/api/webhooks-admin</code>. Exemptions now match a path exactly or at a <code>/</code> boundary.</li>\n<li><strong>Security: oversized no-JS form POSTs fail loud.</strong> A form-encoded POST whose body exceeded the CSRF buffer cap (1 MB) was silently truncated and replayed to your handler. Such requests now return <code>413</code> and ask for the token via the <code>X-CSRF-Token</code> header instead of corrupting the payload.</li>\n<li><strong>Fix: <code>&lt;Script&gt;</code>/<code>&lt;Image&gt;</code> boolean attributes written as strings.</strong> <code>defer=&quot;false&quot;</code>, <code>async=&quot;no&quot;</code>, <code>priority=&quot;0&quot;</code>, <code>lazy=&quot;false&quot;</code> and similar quoted-falsy forms were coerced to <code>True</code> (the opposite of the source). They now coerce correctly.</li>\n<li><strong>Fix: custom <code>csrf.cookieName</code> / <code>csrf.headerName</code> now reach the client runtime.</strong> Configuring either name made every action POST fail with <code>403</code> — the middleware set and validated the cookie under the configured name while <code>useAction</code>/<code>&lt;Form&gt;</code> kept reading a literal <code>pyxle-csrf</code> cookie and sending a literal <code>x-csrf-token</code> header. Non-default names are now injected into the document shell as <code>window.__PYXLE_CSRF_COOKIE__</code> / <code>window.__PYXLE_CSRF_HEADER__</code> and the client runtime resolves them with the defaults as fallback, in both dev and production builds.</li>\n<li><strong>Fix: no double loader run on hover-then-click navigation.</strong> When a <code>&lt;Link&gt;</code> was prefetched on hover/viewport and then clicked, the click raced a second request and ran the page&#39;s <code>@server</code> loader twice. The click now reuses the in-flight prefetch, and a navigation-sequence token discards a superseded prefetch so a slow one can&#39;t render a stale page over a newer click.</li>\n<li><strong>Multi-core serving: <code>pyxle serve --workers N</code>.</strong> Production serving can now run N independent server processes on one port (each with its own SSR pool and event loop) — throughput scales with cores, no load balancer or shared state to configure. See <a href=\"/docs/guides/deployment#multi-core-worker-processes\">Deployment → Multi-core (worker processes)</a>.</li>\n<li><strong>Sync API endpoints.</strong> A plain <code>def endpoint(request)</code> in <code>pages/api/</code> now runs in Starlette&#39;s threadpool instead of failing at request time — blocking database drivers and sync SDKs no longer require manual <code>asyncio.to_thread</code> wrapping. Sync <code>get</code>/<code>post</code>/… methods on <code>HTTPEndpoint</code> classes are threadpooled too, and <code>HTTPEndpoint</code> classes dispatch correctly alongside route hooks. See <a href=\"/docs/guides/api-routes#sync-endpoints-and-blocking-calls\">API Routes → Sync endpoints and blocking calls</a>.</li>\n<li><strong>In-memory static asset cache.</strong> When serving a production build, small static files (≤1 MB each, 32 MB total per process) are loaded into memory at startup and served without filesystem I/O or threadpool hops — several-fold higher static throughput. Conditional requests (<code>ETag</code>/<code>If-Modified-Since</code> → 304) and cache headers behave exactly as before; larger files keep streaming from disk.</li>\n</ul>\n<h2 id=\"042\">0.4.2</h2><ul>\n<li><strong>Live dev-server reconciliation.</strong> Editing a <code>.pyxl</code> in <code>pyxle dev</code> now applies route-<em>shape</em> changes without a restart — rename/add/remove a <code>@server</code> loader or <code>@action</code>, add or delete a page, wrap a page in a layout, change the head. The route table is rebuilt and hot-swapped on every change (these previously needed <code>rm -rf .pyxle-build</code> + a restart). Editing <code>pyxle.config.json</code> prints a clear &quot;restart to apply&quot; warning instead of being silently ignored.</li>\n<li><strong><code>pyxle check</code> works on a clean install.</strong> The JSX checker&#39;s parser dependencies are bundled into a self-contained extractor (shipped via <code>pyxle-langkit</code>), so <code>pyxle check</code> runs after <code>pip install &#39;pyxle-framework[langkit]&#39;</code> with no extra npm setup. A missing-dependency failure now names the exact packages instead of an opaque error.</li>\n<li><strong>Locale-independent SSR.</strong> The Python↔Node SSR transport pins UTF-8, so emoji and other non-BMP characters in components or loader data no longer crash rendering under a non-UTF-8 locale (e.g. <code>LANG=C</code>).</li>\n<li><strong>Smoother first run.</strong> <code>pyxle init</code> generates a gitignored <code>.env.local</code> with a random dev <code>PYXLE_SECRET_KEY</code> (CSRF HMAC on out of the box); the scaffold <code>requirements.txt</code> now declares <code>pyxle-framework</code> itself; and <code>pyxle install</code> warns about PEP 668 externally-managed environments with venv guidance instead of letting pip throw a raw wall.</li>\n<li><strong>Docs:</strong> documented calling an <code>@action</code> endpoint directly for scripts and tests.</li>\n</ul>\n<h2 id=\"041\">0.4.1</h2><ul>\n<li><strong>No more double loader run on first load.</strong> The page you land on is now seeded into the client navigation cache from the server render, so the active nav link&#39;s prefetch (a <code>&lt;Link&gt;</code> pointing at the current route) resolves from cache instead of re-fetching. The loader runs <strong>once</strong>, not twice — halving origin work on cold loads and stopping non-idempotent loaders (view counters, analytics) from firing twice. Back/forward navigation to the page is instant for the same reason.</li>\n<li><strong>Per-route navigation-cache TTL.</strong> A route&#39;s <a href=\"/docs/reference/configuration#edge-caching\">`cache`</a> TTL now also governs how long its prefetched/seeded data stays fresh in the client navigation cache, so client freshness matches how long a CDN would serve the page. Routes without a <code>cache</code> entry use a default of <strong>2 minutes</strong> (raised from 30s), tunable via the new <a href=\"/docs/reference/configuration#navigation\">`navigation.defaultPrefetchTtl`</a> option.</li>\n</ul>\n<h2 id=\"040\">0.4.0</h2><ul>\n<li><strong>Edge caching.</strong> Declare cacheable routes in <a href=\"/docs/reference/configuration#edge-caching\">`pyxle.config.json::cache`</a> and pages are served <code>Cache-Control: public, s-maxage=N</code> (plus <code>stale-while-revalidate</code>) so a CDN or reverse proxy can absorb traffic instead of your origin — Pyxle&#39;s config-driven take on per-route revalidation. The per-user CSRF cookie is automatically omitted from cacheable responses. See <a href=\"/docs/guides/deployment#cdn-and-edge-caching\">Deployment → CDN and edge caching</a>.</li>\n<li><strong>Hardened production errors.</strong> SSR render failures — including the SPA-navigation JSON path — are sanitized in the response (no exception type, message, or path leaks to the client) while the full error is written to the server log for operators.</li>\n<li><strong>Faster static serving.</strong> The static-asset middleware indexes public/client paths up front, skipping a per-request filesystem <code>stat</code> + exception on every dynamic request.</li>\n<li><strong>Layout &amp; template loaders.</strong> A <code>layout.pyxl</code> or <code>template.pyxl</code> can declare its own <code>@server</code> loader; its result lands on the component&#39;s <code>data</code> prop, just like a page — so shared UI (nav bars, the signed-in user, the framework version) loads once per request without repeating the loader in every page. See <a href=\"/docs/core-concepts/layouts#layout-data-loaders\">Layouts → Layout data loaders</a>.</li>\n</ul>\n<h2 id=\"030\">0.3.0</h2><ul>\n<li><strong>First-class plugin system.</strong> Compose apps via <code>pyxle.config.json::plugins</code> (Django-style <code>INSTALLED_APPS</code>) — see the <a href=\"/docs/guides/plugins\">Plugins guide</a> and <a href=\"/docs/reference/plugins-api\">Plugins API reference</a>.</li>\n<li><strong>Django-style service access.</strong> Resolve any plugin-registered service with <code>from pyxle.plugins import plugin</code> and <code>plugin(&quot;auth.service&quot;)</code>, or use a typed shortcut shipped by the plugin (e.g. <code>from pyxle_auth import get_auth_service</code>).</li>\n<li><strong>First-party plugins.</strong> Two official plugins land: <a href=\"/docs/plugins/pyxle-db\">`pyxle-db`</a> (SQLite-first with migrations) and <a href=\"/docs/plugins/pyxle-auth\">`pyxle-auth`</a> (email+password sessions, argon2id, rate limits).</li>\n<li><strong>WebSocket endpoints</strong> — <code>pages/api/*.py</code> can export <code>async def websocket(ws)</code> for live updates, chat, log streaming. See the <a href=\"/docs/guides/api-routes#websocket-endpoints\">API Routes guide</a>.</li>\n<li><strong>Client navigation cache with TTL + invalidation.</strong> Loader payloads are cached for 30s by default (tunable) so back/forward navigation is instant. Call <a href=\"/docs/reference/client-api#invalidateurl\">`invalidate(url)`</a> from the client or return <a href=\"/docs/reference/runtime-api#invalidate_routesresponse-urls\">`invalidate_routes(response, ...)`</a> from an <code>@action</code> to keep list views fresh after mutations — automatically honoured by <code>useAction</code> and <code>&lt;Form&gt;</code>.</li>\n<li><strong><code>ActionError</code> is auto-imported</strong> for any <code>.pyxl</code> with an <code>@action</code>. No more <code>NameError: name &#39;ActionError&#39; is not defined</code> on first try.</li>\n<li><strong><code>&lt;Head&gt;</code> coerces multi-part <code>&lt;title&gt;</code> children</strong> into a single string, silencing React&#39;s &quot;title element received an array&quot; warning for the common <code>&lt;title&gt;{name} — Brand&lt;/title&gt;</code> pattern.</li>\n<li><strong>SSR worker pins <code>LANG=en-US.UTF-8</code></strong> by default (override with <code>PYXLE_SSR_LOCALE</code>) so <code>toLocaleString()</code> and other Intl calls stop causing hydration mismatches.</li>\n<li><strong>Vite resolver prefers pinned versions.</strong> <code>pyxle build</code> now runs <code>npm install</code> before falling back to <code>npx --yes vite</code>, so builds honour your <code>package.json</code> pin instead of fetching <code>vite@latest</code>.</li>\n</ul>\n","toc":[{"depth":2,"text":"Unreleased","slug":"unreleased"},{"depth":2,"text":"0.4.5","slug":"045"},{"depth":2,"text":"0.4.4","slug":"044"},{"depth":2,"text":"0.4.3","slug":"043"},{"depth":2,"text":"0.4.2","slug":"042"},{"depth":2,"text":"0.4.1","slug":"041"},{"depth":2,"text":"0.4.0","slug":"040"},{"depth":2,"text":"0.3.0","slug":"030"}],"path":"changelog","markdown":"# Changelog\n\nRelease notes for Pyxle. While we're in beta (`0.x`), minor versions may include breaking changes — those are called out explicitly here. To upgrade, run `pip install --upgrade pyxle-framework`.\n\n## Unreleased\n\n- **OpenTelemetry tracing (opt-in).** Emit OpenTelemetry spans for each request plus child spans for the SSR render, `@server` loaders, and `@action` calls, so a slow page surfaces as a drill-down trace. It's the heaviest integration, so it's a **separate** optional extra (`pip install \"pyxle-framework[observability-otel]\"`) and **fully off** by default — with the extra absent, spans are a zero-cost no-op (one boolean check), and the exporter endpoint comes from the standard `OTEL_EXPORTER_OTLP_ENDPOINT` env var. Sampling defaults to 5%. A base Pyxle install pulls in no OpenTelemetry packages; enabling `observability.otel` without the extra fails loudly at startup. See [Observability → OpenTelemetry tracing](guides/observability.md#opentelemetry-tracing).\n- **`pyxle dev --dashboard` — a live terminal observability panel.** Opt into a compact panel that refreshes every few seconds with request throughput and error rate, status-class breakdown, request/SSR-render/loader/action latency, and the page-cache hit ratio. It reads the same in-process metrics (so it's free on the request path), is **dependency-free** (no `rich`), and is dev-only — in production you'd scrape the metrics endpoint instead. See [Observability → Dev dashboard](guides/observability.md#dev-dashboard).\n- **Structured access logging.** Opt into one structured log line per request — `method`, `path`, `status`, `duration_ms`, and the request's correlation id — via `observability.accessLog`, in `\"console\"` (human-readable) or `\"json\"` format. The correlation id is bound into a context variable for the request's duration, so your own log calls pick it up too. Works with **no new dependency** (a stdlib JSON/console formatter); the optional `[observability]` extra adds [structlog](https://www.structlog.org/) for richer rendering. Off by default so it won't disrupt an existing log pipeline. See [Observability → Structured logging](guides/observability.md#structured-logging).\n- **Observability: request IDs, timing, metrics, and richer health probes.** Every request now gets a correlation id — surfaced as the `X-Request-Id` response header and on `request.state.request_id`, readable from any `@server` loader or `@action` — and its duration is measured, along with SSR-render, loader, action, and page-cache (hit/stale/miss) metrics. The request-id and timing are **on by default** (a `uuid4` and two `perf_counter` reads, no I/O, added at the outermost layer so even a CSRF rejection is tagged); an incoming `X-Request-Id` is **not** trusted unless you opt in (`observability.trustIncomingRequestId`), since echoing a client value into logs is a spoofing vector. Metrics are recorded into a bounded, allocation-cheap in-process registry and exposed — **opt-in** — at a Prometheus endpoint (`observability.metricsEndpoint`, optionally bearer-guarded); the page-cache hit ratio comes from the existing server cache. `/readyz` now runs **dependency checks** (today: the SSR worker pool has a live worker) and returns `503` until the server is genuinely ready, while `/healthz` stays a pure liveness probe. All configured under the new [`observability`](reference/configuration.md#observability) block (`\"observability\": false` disables request-id/timing); the helper `from pyxle.observability import get_request_id` reads the id anywhere. **Multi-worker note:** metrics are per-worker (each series carries a `worker` label) — aggregate at the scraper. Structured logging, a `pyxle dev` dashboard, and OpenTelemetry land next in Phase 2.7. See [Observability](guides/observability.md).\n- **Typed request validation for `@action` with Pydantic.** An action can now declare a body parameter annotated with a [Pydantic](https://docs.pydantic.dev/) model — `async def register(request, body: Signup)` — and Pyxle parses, coerces, and validates the request body before the action runs, passing in a fully-typed instance. No more hand-written `await request.json()` + key-by-key checks. On failure the action is **not** called and the client gets a `422` with a top-level `fields` map (field path → messages, dotted for nested models and indexed for list items: `address.zip`, `tags.0`). Both `useAction` and `<Form>` surface it — `result.fields` / the reactive `submit.fields`, and `<Form onError={(message, fields) => …}>` — so you can render messages next to each input. For checks Pydantic can't express (uniqueness, cross-field rules) raise the new `ValidationActionError`, which is auto-imported alongside `ActionError` and produces the same `422` shape. Because the body models are real Pydantic models, `pyxle openapi` generates an [OpenAPI 3.1](https://spec.openapis.org/oas/v3.1.0) document of every action's request body straight from your code (`--out openapi.json`), derived from runtime introspection so it always matches what the dispatcher validates. Pydantic is an **optional dependency** — install the extra with `pip install \"pyxle-framework[pydantic]\"`; actions without a model parameter are unaffected, and an action that declares one without Pydantic installed fails with a clear error rather than silently skipping validation. The generated `useAction`/`Form` runtimes now also ship TypeScript declarations (`.d.ts`). See [Server Actions → Validating request bodies](core-concepts/server-actions.md#validating-request-bodies-with-pydantic).\n- **Fix: a server module that fails to import is no longer cached as a broken empty module.** If a page's compiled server module raised during its first import (e.g. a bad top-level `import`), the half-initialised module was left in `sys.modules`, so every later request for that page silently got an *empty* module instead of re-raising the real error. Failed imports are now evicted, so the error surfaces consistently on each request.\n- **WebSockets — page handlers, a client hook, and pub/sub.** A page `.pyxl` can now export a module-scope `async def websocket(ws)` and Pyxle serves a WebSocket route at the page's path, alongside its HTTP render — the same file handles both protocols, and dynamic segments (`pages/chat/[room].pyxl`) resolve into `ws.path_params`. Detection is by convention (the function name, not a decorator), matching how `pages/api/**` modules already expose a `websocket` callable. The new `pyxle.realtime` module ships a small pub/sub layer: `async with channel(ws, \"room\") as room: await room.publish(msg)` subscribes a connection to a named channel and broadcasts to everyone in it, with stale-connection pruning. Because Starlette runs HTTP middleware only for `http` scope, a WS handler gets **no** middleware — so `pyxle.realtime` also provides `authenticate_websocket(ws)` (resolve the pyxle-auth session from the handshake cookie) and `origin_allowed(ws, origins)` (the WS equivalent of CSRF). On the client, the generated `useWebSocket(path, options?)` hook gives `{ status, send, lastMessage, error }` with JSON parsing and exponential-backoff reconnect, and never opens a socket during SSR. **Multi-worker caveat (called out honestly):** the default in-process broker lives in one process, so under `pyxle serve --workers N` a message published on one worker isn't seen by clients on another — Pyxle logs a warning when it detects this, and the `Broker` protocol lets a Redis-backed broker drop in. See [WebSockets](guides/websockets.md).\n- **Streaming SSR for `<Suspense>` pages.** A page that wraps a slow part in a React 18 `<Suspense>` boundary now streams: Pyxle flushes the shell (everything outside the boundary) immediately for a fast time-to-first-byte, then streams each boundary's content in as it resolves. It's opt-in and zero-config — the compiler detects `<Suspense>` and only those pages stream; every other page keeps the buffered render byte-for-byte. The page hydrates exactly as before (the browser hydrates the same component), so no app changes are needed beyond adding the boundary. Streaming applies to dynamic, per-request pages only: a publicly cacheable route (`{\"data\", \"revalidate\"}` envelope, `CACHE` directive, or edge `cache` entry) still renders buffered so it can be stored and `ETag`'d, and a streamed response is always `private, no-cache`. The document head is flushed before the component renders, so a streamed page uses its static `HEAD` (a runtime-rendered `<Head>` lands too late — buffered pages are unaffected). A render error before the first byte falls back to the nearest `error.pyxl` rather than emitting a partial document. Built on the Node worker's new multi-frame `renderToPipeableStream` transport. See [Streaming SSR](guides/streaming.md).\n- **`loading.pyxl` route-level loading states.** A `loading.pyxl` file declares a loading component that wraps the whole route in `<Suspense fallback={<Loading/>}>` — streamed as the shell on the server and applied identically on the client so hydration matches. The nearest `loading.pyxl` wins (directory walk-up, like `error.pyxl`), and it composes with a page's own inner `<Suspense>`. Honest caveat: because the `@server` loader runs before render, the fallback only appears when the *render* suspends (a child using `React.lazy`, `use()`, or a thrown promise) — a page whose data comes entirely from its loader never suspends and streams at once. See [Streaming SSR → Route-level loading states](guides/streaming.md#route-level-loading-states-with-loadingpyxl).\n- **Server-side page caching with incremental regeneration.** A `@server` loader can now return a `{\"data\": ..., \"revalidate\": N}` envelope to cache its rendered HTML for `N` seconds — later requests are served from the cache without re-running the loader or the Node SSR render. A loader-less page can opt in the same way with a module-level `CACHE = {\"revalidate\": N}` directive. And `pyxle build --static` pre-renders every loader-less, non-dynamic page to HTML at build time and warms the cache from it on startup, so the first request after a deploy is a hit — no cold render. Once the window passes, the stale page is served instantly while a single background re-render refreshes it (incremental static regeneration — no thundering herd). Purge a route on demand from an action with `await cache.invalidate(\"/path\")` (or `cache.invalidate_all()`). Routes already declared in the `pyxle.config.json` edge `cache` block are server-cached automatically under the same TTL. Cached responses carry a strong `ETag` (so conditional requests get a `304`) and an `x-pyxle-cache: HIT|STALE|MISS` header. The cache is enabled for `pyxle serve` (off in `pyxle dev`) and stores to a bounded in-memory backend by default, or to local disk or a shared Redis via `PYXLE_PAGE_CACHE_BACKEND` (Redis needs `pip install 'pyxle-framework[redis]'`). Only cache pages that render no per-user data — a cached render is shared with every visitor. See [Caching](guides/caching.md).\n\n## 0.4.5\n\n- **Signed cookies & tokens — `sign_cookie` / `verify_cookie`.** New stdlib-only helpers (importable from `pyxle`) attach a tamper-proof HMAC-SHA256 signature to any string — a session id, a \"remember me\" token, a stateless unsubscribe or password-reset link — and verify it later in constant time. The secret comes from `PYXLE_SECRET_KEY` (or an explicit `secret_key=`), and a `salt=` namespaces signatures so a token minted for one purpose can't be replayed for another. Signing without a secret raises `MissingSecretKeyError` rather than returning an unprotected value — it fails closed. See [Security → Signed cookies and tokens](guides/security.md#signed-cookies-and-tokens).\n- **Fix: `PYXLE_PUBLIC_*` client env vars now work in `pyxle dev` and no longer break `pyxle build`.** The generated Vite `define` emitted each value as a bare expression, so a non-identifier value (an API URL, a `0x…` Turnstile key) was silently dropped in dev and crashed `vite build` with esbuild's \"Invalid define value\". Values are now emitted as quoted JS string literals, so `import.meta.env.PYXLE_PUBLIC_*` resolves correctly in both dev and production builds. See [Environment variables → Client-side variables](guides/environment-variables.md).\n- **Fix: web fonts and other CSS `url()` assets no longer 404 in `pyxle dev`.** Vite rewrites `@font-face`/`url(...)` references to root-relative `/@fs/…` paths, which the browser resolved against Pyxle's origin instead of Vite's and 404'd — so `@fontsource` `.woff2` files silently failed to load in development (production builds were unaffected, as their assets are hashed and served by Pyxle). Pyxle now sets Vite's `server.origin`, so dev assets load from Vite directly.\n\n## 0.4.4\n\n- **Fix: cross-page hash links scroll to their anchor.** Client-side navigation to `/page#section` changed the page but stayed pinned to the top (only the URL carried the hash); a full load scrolled correctly. The router now jumps to the top and then scrolls to the anchor as soon as the next page's DOM commits, matching native behaviour — including for unknown anchors, which stay at the top.\n\n## 0.4.3\n\n- **Security: hardened HEAD sanitisation (XSS).** Dynamic `HEAD` values/callables that interpolate loader data into raw head strings — the documented dynamic-meta-tags recipe — could previously inject markup because attribute values weren't escaped (a `\"` in a `<meta content>` broke out of the attribute). Head elements are now parsed and rebuilt from a strict tag allowlist with every attribute value HTML-escaped, `on*` handlers dropped, `javascript:`/`vbscript:`/`data:` URLs neutralised, `<meta http-equiv=\"refresh\">` and non-head tags rejected, and any markup injected after a breakout discarded. Inline `<script>`/`<style>` head content remains supported as trusted author code. Self-closing head tags now serialise canonically as `/>`.\n- **Security: CSRF exempt paths match on segment boundaries.** `csrf.exemptPaths` previously used a bare string prefix, so exempting `/api/webhooks` also exempted an adjacently-named sibling like `/api/webhooks-admin`. Exemptions now match a path exactly or at a `/` boundary.\n- **Security: oversized no-JS form POSTs fail loud.** A form-encoded POST whose body exceeded the CSRF buffer cap (1 MB) was silently truncated and replayed to your handler. Such requests now return `413` and ask for the token via the `X-CSRF-Token` header instead of corrupting the payload.\n- **Fix: `<Script>`/`<Image>` boolean attributes written as strings.** `defer=\"false\"`, `async=\"no\"`, `priority=\"0\"`, `lazy=\"false\"` and similar quoted-falsy forms were coerced to `True` (the opposite of the source). They now coerce correctly.\n- **Fix: custom `csrf.cookieName` / `csrf.headerName` now reach the client runtime.** Configuring either name made every action POST fail with `403` — the middleware set and validated the cookie under the configured name while `useAction`/`<Form>` kept reading a literal `pyxle-csrf` cookie and sending a literal `x-csrf-token` header. Non-default names are now injected into the document shell as `window.__PYXLE_CSRF_COOKIE__` / `window.__PYXLE_CSRF_HEADER__` and the client runtime resolves them with the defaults as fallback, in both dev and production builds.\n- **Fix: no double loader run on hover-then-click navigation.** When a `<Link>` was prefetched on hover/viewport and then clicked, the click raced a second request and ran the page's `@server` loader twice. The click now reuses the in-flight prefetch, and a navigation-sequence token discards a superseded prefetch so a slow one can't render a stale page over a newer click.\n- **Multi-core serving: `pyxle serve --workers N`.** Production serving can now run N independent server processes on one port (each with its own SSR pool and event loop) — throughput scales with cores, no load balancer or shared state to configure. See [Deployment → Multi-core (worker processes)](guides/deployment.md#multi-core-worker-processes).\n- **Sync API endpoints.** A plain `def endpoint(request)` in `pages/api/` now runs in Starlette's threadpool instead of failing at request time — blocking database drivers and sync SDKs no longer require manual `asyncio.to_thread` wrapping. Sync `get`/`post`/… methods on `HTTPEndpoint` classes are threadpooled too, and `HTTPEndpoint` classes dispatch correctly alongside route hooks. See [API Routes → Sync endpoints and blocking calls](guides/api-routes.md#sync-endpoints-and-blocking-calls).\n- **In-memory static asset cache.** When serving a production build, small static files (≤1 MB each, 32 MB total per process) are loaded into memory at startup and served without filesystem I/O or threadpool hops — several-fold higher static throughput. Conditional requests (`ETag`/`If-Modified-Since` → 304) and cache headers behave exactly as before; larger files keep streaming from disk.\n\n## 0.4.2\n\n- **Live dev-server reconciliation.** Editing a `.pyxl` in `pyxle dev` now applies route-*shape* changes without a restart — rename/add/remove a `@server` loader or `@action`, add or delete a page, wrap a page in a layout, change the head. The route table is rebuilt and hot-swapped on every change (these previously needed `rm -rf .pyxle-build` + a restart). Editing `pyxle.config.json` prints a clear \"restart to apply\" warning instead of being silently ignored.\n- **`pyxle check` works on a clean install.** The JSX checker's parser dependencies are bundled into a self-contained extractor (shipped via `pyxle-langkit`), so `pyxle check` runs after `pip install 'pyxle-framework[langkit]'` with no extra npm setup. A missing-dependency failure now names the exact packages instead of an opaque error.\n- **Locale-independent SSR.** The Python↔Node SSR transport pins UTF-8, so emoji and other non-BMP characters in components or loader data no longer crash rendering under a non-UTF-8 locale (e.g. `LANG=C`).\n- **Smoother first run.** `pyxle init` generates a gitignored `.env.local` with a random dev `PYXLE_SECRET_KEY` (CSRF HMAC on out of the box); the scaffold `requirements.txt` now declares `pyxle-framework` itself; and `pyxle install` warns about PEP 668 externally-managed environments with venv guidance instead of letting pip throw a raw wall.\n- **Docs:** documented calling an `@action` endpoint directly for scripts and tests.\n\n## 0.4.1\n\n- **No more double loader run on first load.** The page you land on is now seeded into the client navigation cache from the server render, so the active nav link's prefetch (a `<Link>` pointing at the current route) resolves from cache instead of re-fetching. The loader runs **once**, not twice — halving origin work on cold loads and stopping non-idempotent loaders (view counters, analytics) from firing twice. Back/forward navigation to the page is instant for the same reason.\n- **Per-route navigation-cache TTL.** A route's [`cache`](reference/configuration.md#edge-caching) TTL now also governs how long its prefetched/seeded data stays fresh in the client navigation cache, so client freshness matches how long a CDN would serve the page. Routes without a `cache` entry use a default of **2 minutes** (raised from 30s), tunable via the new [`navigation.defaultPrefetchTtl`](reference/configuration.md#navigation) option.\n\n## 0.4.0\n\n- **Edge caching.** Declare cacheable routes in [`pyxle.config.json::cache`](reference/configuration.md#edge-caching) and pages are served `Cache-Control: public, s-maxage=N` (plus `stale-while-revalidate`) so a CDN or reverse proxy can absorb traffic instead of your origin — Pyxle's config-driven take on per-route revalidation. The per-user CSRF cookie is automatically omitted from cacheable responses. See [Deployment → CDN and edge caching](guides/deployment.md#cdn-and-edge-caching).\n- **Hardened production errors.** SSR render failures — including the SPA-navigation JSON path — are sanitized in the response (no exception type, message, or path leaks to the client) while the full error is written to the server log for operators.\n- **Faster static serving.** The static-asset middleware indexes public/client paths up front, skipping a per-request filesystem `stat` + exception on every dynamic request.\n- **Layout & template loaders.** A `layout.pyxl` or `template.pyxl` can declare its own `@server` loader; its result lands on the component's `data` prop, just like a page — so shared UI (nav bars, the signed-in user, the framework version) loads once per request without repeating the loader in every page. See [Layouts → Layout data loaders](core-concepts/layouts.md#layout-data-loaders).\n\n## 0.3.0\n\n- **First-class plugin system.** Compose apps via `pyxle.config.json::plugins` (Django-style `INSTALLED_APPS`) — see the [Plugins guide](guides/plugins.md) and [Plugins API reference](reference/plugins-api.md).\n- **Django-style service access.** Resolve any plugin-registered service with `from pyxle.plugins import plugin` and `plugin(\"auth.service\")`, or use a typed shortcut shipped by the plugin (e.g. `from pyxle_auth import get_auth_service`).\n- **First-party plugins.** Two official plugins land: [`pyxle-db`](plugins/pyxle-db.md) (SQLite-first with migrations) and [`pyxle-auth`](plugins/pyxle-auth.md) (email+password sessions, argon2id, rate limits).\n- **WebSocket endpoints** — `pages/api/*.py` can export `async def websocket(ws)` for live updates, chat, log streaming. See the [API Routes guide](guides/api-routes.md#websocket-endpoints).\n- **Client navigation cache with TTL + invalidation.** Loader payloads are cached for 30s by default (tunable) so back/forward navigation is instant. Call [`invalidate(url)`](reference/client-api.md#invalidateurl) from the client or return [`invalidate_routes(response, ...)`](reference/runtime-api.md#invalidate_routesresponse-urls) from an `@action` to keep list views fresh after mutations — automatically honoured by `useAction` and `<Form>`.\n- **`ActionError` is auto-imported** for any `.pyxl` with an `@action`. No more `NameError: name 'ActionError' is not defined` on first try.\n- **`<Head>` coerces multi-part `<title>` children** into a single string, silencing React's \"title element received an array\" warning for the common `<title>{name} — Brand</title>` pattern.\n- **SSR worker pins `LANG=en-US.UTF-8`** by default (override with `PYXLE_SSR_LOCALE`) so `toLocaleString()` and other Intl calls stop causing hydration mismatches.\n- **Vite resolver prefers pinned versions.** `pyxle build` now runs `npm install` before falling back to `npx --yes vite`, so builds honour your `package.json` pin instead of fetching `vite@latest`.\n","prev":{"title":"Frequently Asked Questions","path":"faq"}}